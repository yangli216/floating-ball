# 智云诊·浮舱 (SmartClin Capsule) —— 基层医疗 AI 辅助工作站

**智云诊·浮舱** 是一款专为基层医生量身定做的桌面端 AI 医疗辅助工作站。产品突破了传统 HIS 系统的封闭和繁琐，以“无边框透明悬浮球”的形式静默运行于桌面，不干扰医生现有的任何工作流。

产品坚持“医生主导、AI 辅助”原则，保留原有症状问诊主流程作为默认入口，同时新增面向基层门诊的轻量触发式辅助模式作为附加入口。医生既可以沿用完整问诊链路，也可以在已接诊患者上下文中通过额外入口唤起一个桌面助手式悬浮小窗，按需触发主诉/病史生成、推荐诊断、诊断鉴别、推荐用药、推荐检查检验与智能提醒，并将结果一键回写到医生站草稿。

## 主要功能点

1. **沉浸式桌面交互（原生无感体验）**
   - **透明悬浮球置顶**：基于跨平台框架打造，以极小的系统资源占用悬浮于任何系统或 HIS 软件之上，双击唤起，随叫随到。
   - **轻量悬浮 session**：灵活触发场景下以桌面助手式悬浮小窗承载当前患者本次就诊辅助，采用折叠分段和底部动作坞，不抢占医生站主工作区。

2. **双轨就诊模式（完整流程 + 灵活触发）**
   - **完整流程保留**：现有症状采集、属性问诊、主诉/现病史生成能力继续保留，并且仍然是 HIS 接诊后的默认进入方式。
   - **模块化触发**：医生可在已接诊患者上下文中通过附加入口按需触发单个 AI 模块，避免强制线性流程与重复录入。

3. **结构化与智能引导问诊（AI 驱动的症状网络）**
   - **智能病历引擎**：预置内外妇儿多科室标准症状模板（支持中医/西医双模式一键切换），具备“伴随症状智能推荐”和“单选/多选/跳过”等复杂逻辑判定体系。
   - **自动病历生成**：医生仅需简单勾画点击，系统应用内置的自然语言生成算法，秒级自动拼接出符合标准规范的“主诉”和“现病史”，并允许医生持续编辑而不打断后续推荐。

4. **大模型结构化引擎数据映射**
   - **打通自由文本与业务系统的壁垒**：为了将大模型生成的自由文本型诊断/用药建议直接应用到医院现有的生产系统（HIS）中，内置了高性能的数据匹配机制。
   - **精准字典映射**：产品能毫秒级提取大模型给出的非结构化描述，并在自带的 20,000+ 基础标准数据库（`diagnoses.csv`/`medicines.csv`/`items.csv`）中完成词林映射，最终向生产系统输出标准化的结构数据草稿。

5. **AI 临床决策辅助 (CDSS)**
   - **大模型私有化接入**：支持无缝对接兼容 OpenAI 协议的各类大语言模型（如 DeepSeek 等）。
   - **智能推理**：面对不典型症状，AI 能够通过解析对话上下文提供高质量的诊断鉴别建议、开药指导和检查建议。
   - **建议可编辑**：所有 AI 推荐仅作为草稿，支持编辑、删除、替换，且修改后不中断当前 session。

## 赋能基层医疗的核心场景

在基层医疗机构（如乡镇卫生院、社区卫生服务中心、村镇诊所），医生往往面临**全科化挑战大、时间紧凑、病历书写不规范、上级指导缺乏**等痛点。本产品主要在以下场景深度赋能：

### 特性 1：破解“看不了、看不准”的全科诊断难题
- **场景痛点**：基层全科医生知识面广但深度受限，特别是遇到相对生僻或跨科室症状时容易漏诊误诊。
- **赋能点**：当输入核心症状时，系统会自动进行**“伴随症状智能推荐”**，提示医生沿着推荐链路进行深度问诊排查；遇到疑难杂症，一键呼叫 AI 助手给出多维度的鉴别诊断。产品充当了一台全天候的“规培带教机器”和专家智库。

### 特性 2：拯救“耗时、易错”的病历书写泥潭
- **场景痛点**：每天面对庞大的就诊量，手敲病历耗费大量精力，且主诉和现病史的格式经常被质控部门打回。
- **赋能点**：通过“点选式”的高效问诊收集，智能文本引擎根据医学标准规范，巧妙剔除无效对话，并自动补充核心要点（如精确带有时长的“主诉”拼接）。医生既可按完整链路一次生成，也可在医生站内按需触发生成并直接回写 HIS 草稿，将原本 3 分钟的书写时间压缩至 10 秒，腾出更多精力用于医患沟通。

### 特性 3：不打断门诊节奏的模块化辅助
- **场景痛点**：基层门诊接诊节奏快，医生常常只需要某一类建议，而非被迫走完整套 AI 流程。
- **赋能点**：浮舱以“助手面板 + 折叠分段”的方式记录本次就诊辅助。每次触发只聚焦当前任务，但多个 AI 模块会按固定顺序沉淀成可展开 / 收起的分段，医生可只查看当前需要的一段；底部常态压缩为单行状态栏，只有在需要时才向上展开快捷动作，医生可快速采纳并回写医生站草稿，需要深度补录时再进入完整问诊。
- **可理解性要求**：患者基础信息只作为原始上下文展示；AI 生成的主诉、现病史必须在“主诉病史”模块中单独高亮展示，让医生一眼分清“原始信息”和“AI 草稿”。
- **交互约束**：对诊断、用药、检查等推荐，医生在候选列表中完成选择后，确认动作应直接完成“采纳并回写”，避免再走一次底部回写按钮。
- **回写约束**：即便本次只采纳了用药或检查建议，没有先生成主诉病史或诊断，系统也应允许回写，不得额外拦截。
