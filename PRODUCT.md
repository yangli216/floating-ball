# 全医慧助（PCIE）—— 基层医疗智能专家辅助工作站

**全医慧助**（英文全称 **Primary Care Intelligent Expert**，简称 **PCIE**）是一款面向基层医生的桌面端智能专家辅助工作站。产品突破传统 HIS 系统的封闭和繁琐，以“无边框透明悬浮球”的形式静默运行于桌面，不干扰医生现有工作流。

产品坚持“医生主导、AI 辅助”原则，保留原有症状问诊主流程作为默认入口，同时将原灵活模式入口收敛进同一条智能问诊链路。医生既可以从症状采集开始完整问诊，也可以在已接诊患者上下文中，带着 PHIS 已有的主诉、现病史和诊断信息直接跳入病历详情页、推荐诊断页、诊断鉴别页或用药/检查页；对于检验检查报告解读这类不应打断当前问诊编辑的独立分析任务，则允许以单独结果窗口承载。

## 主要功能点

1. **沉浸式桌面交互（原生无感体验）**
   - **透明悬浮球置顶**：基于跨平台框架打造，以极小的系统资源占用悬浮于任何系统或 HIS 软件之上，双击唤起，随叫随到；启动恢复历史位置时按当前显示器 `workArea` 吸附到左右边缘并完整落在可见安全区域内，避免多屏切换、分辨率变化、Dock / 任务栏遮挡或虚拟桌面切换后不可见；小球状态下环绕悬浮按钮只跟随鼠标悬停显示，鼠标移出小球交互范围后必须隐藏，键盘或点击焦点不得单独保持按钮展开。
   - **统一键盘操作**：所有可点击动作必须优先使用原生 `button / input / select` 等语义元素，并提供清晰的 `:focus-visible` 焦点；悬浮球聚焦后按 Enter 或 Space 等同于双击唤起/恢复，问诊分类、症状、接诊胶囊关闭/风险展开以及设置内入口必须可通过 Tab 定位并用 Enter/Space 操作。应用内全局快捷键默认使用 `Cmd/Ctrl+Shift+Space` 切换悬浮球/当前工作区、`Cmd/Ctrl+Shift+C` 打开对话、`Cmd/Ctrl+,` 打开设置；快捷键仅在应用窗口聚焦时生效，输入框、可编辑区、输入法组合态、模态框和强制更新门禁不得被全局快捷键截获。医生可在通用设置中重新录入、清除或恢复默认值；重复组合和系统/编辑保留组合必须当场拒绝并说明原因。
   - **聊天面板阅读比例**：聊天入口默认采用窄而高的侧边助手比例，优先保证欢迎区、消息流和输入区在首屏内自然分布；不得回退为过宽过矮的横向扁窗，避免欢迎卡片与输入区互相挤压或遮挡。
   - **智能问诊窗口比例**：智能问诊主界面默认采用适度放大的双栏工作台比例，保留病历详情与诊断 / 治疗建议的并排扫描能力，并给结果编辑区和底部操作区预留足够初始空间，同时避免窗口横向铺满屏幕；低于当前默认尺寸的历史记录不得继续作为首选恢复尺寸。
   - **窗口过渡与可见工作区**：悬浮球、接诊胶囊、语音胶囊和各类工作台之间切换时，必须按“旧内容淡出 → 基于当前显示器 `workArea` 裁剪并应用窗口几何 → 切换页面 → 新内容淡入”的统一时序执行；不得先在旧尺寸中渲染新页面，也不得让业务组件直接调用 `setSize / setPosition`。历史窗口尺寸只能作为偏好值，恢复时必须重新限制在当前显示器工作区内并保留安全边距；胶囊形态保持不可自由缩放，大工作台才允许医生调整尺寸。结束就诊必须把 `160×160` 球态作为统一过渡队列的终态屏障：球态提交前完成几何与最小尺寸设置，提交后拒绝旧会话迟到的胶囊/语音阶段 resize，任何情况下都不得先显示小球再留下小于球态的原生窗口。接诊风险详情高度按可见风险行和操作入口动态计算，短列表不得固定撑出大块空白，长列表在最大高度内滚动。Windows 关闭系统动画时遵循 `prefers-reduced-motion`，但仍必须保证几何与内容切换顺序正确。
   - **单窗口灵活模式**：灵活模式场景不再额外打开悬浮小窗，而是直接进入智能问诊主界面的指定阶段；HIS `/assist` 与桌面工具栏入口共用同一条 `ConsultationPage` 灵活模式链路，已具备主诉、现病史时可直接跳过症状采集，减少上下文切换。

2. **统一就诊模式（完整流程 + 内嵌灵活模式）**
   - **完整流程保留**：现有症状采集、属性问诊、主诉/现病史生成能力继续保留，并且仍然是 HIS 接诊后的默认进入方式。
   - **灵活模式**：医生可在已接诊患者上下文中通过附加入口按需触发单个 AI 模块；系统根据 PHIS 已有字段自动判断是否跳过前置步骤，并将医生留在同一问诊页面中继续处理后续动作。

3. **检验检查报告解读（独立结果窗 + 应用内报告工作台）**
   - **双入口复用同一能力**：HIS / SDK 通过 `POST /api/report/interpret` 发起的单份报告解读继续使用独立结果窗口，避免打断正在编辑的问诊或语音结果页；患者接诊后发现近期已出报告时，则从风险胶囊进入应用内“报告助手”工作台。两种入口必须复用同一份报告解析、提示词、异常项提炼、降级解读和报告正文组件，不维护两套解读口径。
   - **近期报告入口**：接诊阶段已经对最近几次历史就诊调用 `loadClinicMedicalRecord`。桌面端应在 HIS Adapter 映射时保留就诊 ID 和 `applyList[].items[]` 中 `sdApply = 3` 的已出报告申请摘要，并按近 14 个自然日筛选；不得为了显示入口重复查询同一批历史病历。仅有 `orderList.sdOrd = 31/41` 的检查检验医嘱不代表报告已出。
   - **统一报告工作台**：工作台左侧按就诊时间组织已出结果的检验检查报告，日期右侧同步显示就诊科室，默认选中最新报告；右侧默认展示当前报告的结构化原始结果。检验按 LIS 报告单聚合，同一 `idReportGroup` 覆盖多个已出结果申请时，工作台必须明确展示“报告单覆盖 N 个申请项目”及申请名称，不能把报告单数量误作开立项目数量。接诊阶段只保存轻量申请摘要，进入工作台后才按历史 `visitId` 获取实际 LIS/PACS 报告内容；AI 解读必须由医生点击“开始 AI 解读”后才发起，生成结果按报告做会话内缓存。报告列表进入工作台时自动加载，不提供容易造成重复请求的手动刷新按钮。
   - **原始报告先于 AI**：医生选中报告后必须立即展示 HIS 返回的原始检验项目或检查所见/结论，并可通过“原始报告 / AI 解读”双视图切换；未触发 AI 时不显示伪 loading 或占位解读。医生手动发起后，生成期间继续保留原始报告，只在其上方显示轻量进度提示；完成后自动切换到完整 AI 解读，医生仍可随时返回原始报告。当前 AI 返回的是完整结构化 JSON，本阶段采用“原始报告即时呈现 + 完整解读一次性落屏”，不直接流式渲染半截 JSON；后续如需流式，只允许为摘要、综合判断等叙述区设计独立增量事件契约。
   - **异常项目确定性优先**：检验异常表只读取 HIS 返回的结构化 `abnormal / direction / abnormalFlag / result / referenceLow / referenceHigh / referenceRange`，并以明确异常标记、阳性/阴性定性结果或数值越界作为判定依据；非正常方向、异常标记、定性阳性结果和数值越界均可纠正冲突的 `direction = normal / abnormal = false`，避免上游聚合误标把阳性结果显示为正常。AI `keyPoints` 仅用于综合判断，不得兜底转换为异常项目。全部指标正常时明确显示“未发现异常项目”，但仍保留原始报告和 AI 对局限性的说明。
   - **解读到回诊的升级路径**：报告解读负责回答“报告说明什么”，默认只读且不产生处方；医生自行决定是否需要调用 AI 解读，简单报告可以只查看原始报告后直接进入后续诊疗方案。每次已执行的解读必须同时形成机器可读的处置结论（无需治疗 / 观察随访 / 需复查或转诊 / 需药物治疗）、问题清单和证据；进入回诊方案时优先消费该结构化结论。未执行解读时，回诊方案以本次病历和结构化原始报告为依据生成，仍由医生编辑确认后才能回写。
   - **上下文增强**：若当前已接诊患者，报告解读默认自动补入当前患者的性别、年龄、既往史/过敏史等可能影响结果解释的信息；若当前没有接诊患者，则允许 HIS / SDK 在入参中显式传入患者基础信息作为解读上下文。
   - **输入保持轻量**：HIS 侧只需传入 `taskId + query` 两个核心字段，`query` 可直接复用现有“报告日期/检查项目/检查结果/影像诊断”的拼接文本；桌面端负责与患者上下文合并并调用 AI。
   - **报告单式阅读**：报告解读窗口采用单页报告单版式，按“报告标题 / 患者与报告元数据 / 异常项目 / 综合判断 / 建议与注意事项”纵向组织，避免三栏卡片造成医生来回扫视。工作台原始报告与 AI 解读共享同一内容宽度、页边距和报告头层级；原始报告优先采用紧凑表格，减少行高和装饰性留白，单项或少量项目报告应尽量在一屏内看全。
   - **重点突出与去重**：摘要只承担首要结论，异常表只承担结构化异常事实，综合判断只展示去重后的临床解释，建议区只展示行动和注意事项；`sections` 中与摘要、`keyPoints` 或建议区重复的“报告核心发现 / 下一步建议”等段落不重复渲染。没有真实病史文本时不以患者姓名、性别、年龄占位展示“病历”行。
   - **总体状态一眼可见**：摘要区以状态带呈现总体结果，按“未见明确异常 / 发现需关注结果 / 存在重点异常”使用绿色、橙色、红色语义和对应图标；状态必须优先由结构化异常项及高风险等级确定，不能仅依赖模型措辞或颜色猜测。纯文本报告缺少完整结构化判断依据且未识别到异常时，使用灰色“结果待确认”，不得误报绿色正常。患者姓名、年龄、性别已在报告头部展示，摘要和结论开头不得重复这些基本信息；年龄等背景只有在影响临床解释时才可在后续判断中出现。
   - **结果只读优先**：报告解读窗口优先强调“摘要结论、报告核心发现、异常/阳性结果、临床意义、建议动作、风险提示、适用范围与局限”，不进入 PHIS 回写闭环，也不修改当前问诊主页面状态。
   - **可读性与交互约束**：报告解读窗口内容超出可视区域时必须由窗口自身提供纵向滚动；窗口默认隐藏原生标题栏并统一使用页面内虚拟关闭按钮，窗口外壳、操作按钮、loading 与空态应沿用 floating-ball 的柔和玻璃态工作台风格；窗口顶部操作只保留打印/导出与关闭，不提供一键复制解读文本入口；打印/导出时必须按报告完整内容自然分页，不得受窗口高度或滚动容器裁切限制；历史报告工作台从 PHIS 结构化结果展示项目、结果、单位、方向和参考范围，外部纯文本入口才使用报告原文解析兜底；解析不到时以占位展示。AI 输出若未给出足够具体的报告结论，桌面端需至少基于报告原文提炼出可直接供医生阅读的“核心发现 + 临床意义 + 下一步建议”。

3A. **门诊回诊（报告复诊）辅助**
   - 医生点击语音问诊时，桌面端通过 HIS Adapter 获取当前就诊的回诊聚合上下文。只要当前就诊能够取得本次门诊病历正文，并且关联到至少一份已经出具且有实际结果内容的检验或检查报告，就优先进入报告工作台；诊断仅作为推荐参考，不再作为进入流程或生成方案的硬前置。语音入口与风险胶囊必须复用同一工作台，不得绕过报告解读直接生成处方。
   - 回诊与复诊配药是两个互斥的门诊意图：当前主诉、现病史或诊断明确表达“携报告/检查结果/化验结果回来复诊、查看结果、报告解读”等语义时，回诊优先级高于慢病续方，不得再展示“复诊配药”候选；只有历史慢病处方证据而没有当前报告回诊语义时，才允许进入复诊配药候选。
   - 当前就诊信息和本次门诊病历文本沿用接诊阶段已经通过 `loadClinicMedicalRecord` 与门诊文书接口获取的数据，不再由报告回诊聚合服务重复读取。PHIS 通过新增 `api/phis.aiInpatientEmrContextService/buildOutpatientFollowUpReportResults` 只返回已出报告的 LIS/PACS 结果；检验报告需保留报告内完整结果项目。桌面端不得在页面组件内拼接多个 PHIS 私有接口。
   - 复诊场景使用独立双栏工作台：左侧按“本次病历 / 报告结果”展示生成依据和异常提示，右侧展示可编辑、可选择的后续诊疗方案。无新增治疗指征、模型成功返回空数组或没有任何可回写项目时，右侧必须展示明确空态、当前结论和“结合原始报告、临床表现决定观察/健康宣教/复查或手动开立”的下一步提示，不得以空白治疗列表代替结论；空态必须明确“本次生成已完成但未给出可回写医嘱”，不得表述为“无需治疗”。生成失败时应展示对应分组错误，不得误用成功空态。AI 推荐仅传入与处置结论相关的病历摘要、异常/结论报告摘要和问题清单；并继续复用现有药品、检查、检验、处置推荐的标准库匹配、库存与必要字段门禁、医生编辑和 `record-confirmed` 回写闭环。
   - 检验检查只认已报告且有实际结果内容的数据。桌面端判断本次是否存在已出报告时读取 `loadClinicMedicalRecord` 返回的 `applyList[].items[].sdApply`，值为 `3` 表示已出报告；不得再用 `orderList.sdOrd` 判断报告状态。PHIS 新服务取检验报告时按 `HI_ODS_APPLY.ID_RESULT = HI_ODS_APPLY_LIS_REPORT.ID_REPORT_GROUP` 关联，取检查报告时按 `HI_ODS_APPLY.ID_APPLY = HI_ODS_APPLY_PACS_REPORT.ID_APPLY` 关联。仅有本次检验检查医嘱、尚未产生报告结果时，不得进入报告回诊方案页，也不得用“0 份报告”的占位上下文抑制普通语音问诊或复诊配药。
   - 风险胶囊不同时堆叠“报告解读”和“报告回诊”两个相似入口，而是统一展示“报告助手”：只有历史报告时显示“报告解读 · 近14天 N 份报告”；本次就诊满足回诊条件时显示“报告回诊 · 本次报告已出”。报告存在但尚未完成内容识别时，胶囊不得使用“健康状态良好”的确定性表达，应显示“暂无已识别风险”。

3B. **接诊后的复诊配药识别**
   - 门诊患者完成健康风险评估后，桌面端按就诊时间读取最近 3 次历史门诊记录。调用 `queryVisitHistory` 时必须传入当前就诊 `idVis`，由 HIS 排除本次诊中记录；只要历史记录中存在受支持的慢病诊断，即判定为复诊配药候选，不强制要求存在历史药品。PHIS 历史药品优先从 `presList[].presSubList[]` 获取结构化处方属性，并按药品分别保留一次剂量、频次、用法、`takeDays`、总量和包装单位；`orderList` 只承担医嘱类型识别、检验检查关联及旧响应兜底，检验检查和处置不得混入历史用药。未取得历史用药时，由模型结合具体慢病诊断和当前有效库存推荐用药方案。
   - 命中时，患者接诊胶囊增加“复诊配药”提示及确认按钮，只展示慢病诊断和“有历史用药参考 / 暂无历史用药参考”状态，不展示“最近几次”或“慢病就诊几次”等统计，因为历史查询窗口不是患者完整就诊次数。慢病分组名称只用于识别候选，不得替换历史病历中的临床诊断；例如历史记录为“2型糖尿病”时，初始诊断建议、病历正文与回写诊断都必须保留“2型糖尿病”，不能降级为“糖尿病”。同组同时存在泛化名称和具体分型时优先采用有历史依据的具体诊断，同等具体程度下采用最近一次；历史记录未提供分型时不得自行推断。历史明确诊断的推荐依据应简洁表达“本次复诊信息 + 近期历史诊断”，不得重复拼接，不得夹带药品、检验检查原始列表或 `null` 等无效值，也不得使用“模型初步考虑”描述已经有历史依据的诊断。医生点击后基于历史慢病诊断、可用历史处方和病史生成可编辑草稿；没有历史药品时，模型直接结合诊断和有效库存生成方案。主诉应表达具体慢病复诊续方目的，现病史应概括既往诊断、已有历史用药或“历史用药待核实”，以及本次待医生核实的病情控制/依从性，不得使用“未提供新发不适信息”作为主要病历内容，也不得臆造“病情稳定”“无不适”。慢病续方模型必须基于可用历史处方、当前有效库存名称与规格返回结构化的单次剂量、剂量单位、频次、用法、天数和总量；字段取值优先级为“历史处方明确值 → 模型结合库存规格生成值 → HIS 药品默认值”。不得用固定 `1`、固定 `14天` 或销售单位冒充临床剂量；仍缺少完整核心用法的药品不得默认选中。推荐依据中的临床理由与处方换算必须分离：模型只说明为什么推荐该药，剂量和总量说明由程序依据最终字段实时计算，例如“单次0.5g（2片）× 每日3次 × 30天 = 180片；60片/瓶，共3瓶”，不得直接展示未经校验的模型算术。
   - 复诊配药只处理慢病续方药品：优先保留当前有效库存中的历史同品，缺货时可推荐库存内临床等效药；若既无同品库存也无合适等效药，则保留规范通用名作为无库存参考，不自动勾选回写。该场景默认不生成检查、检验和处置建议，且共享结果页不得因初始药品为空而回退触发通用四路治疗推荐。
   - 慢病复诊现病史只记录患者事实：既往诊断、本次复诊目的、已知历史用药、病情控制、服药依从性、不良反应和近期监测情况；未知信息可以明确写“待核实”，但当前库存、可续方药品、推荐药品和后续治疗方案不得进入现病史，只能进入右侧治疗方案及处理意见。模型输出若违反该边界，桌面端必须拒绝该段现病史并使用合规规则草稿。
   - 生成结果进入现有可编辑临床结果页，医生可以修改主诉、现病史、既往史、诊断和库存内续方药品，并继续使用标准库匹配、库存门禁与 `record-confirmed` 回写闭环。
   - 医生点击“复诊配药”后先进入动态确认页，不直接生成模板化病历。模型只基于最近慢病就诊、处方和病历文本生成 3～5 个必要确认项及推荐选项；确认项不依赖病种白名单配置，必须携带推荐依据和置信度。推荐项默认选中以减少操作，但只在医生点击“按当前选择生成病历”后转为本次就诊事实；没有证据的“病情平稳、规律服药、无不适”等不得静默写入。
   - 动态确认页支持文字和语音补充，但补充信息不重新生成、不改写确认项，也不触发第二次确认计划调用。语音只负责转写并追加为医生补充说明；文字与语音补充在最终生成时作为本次医生明确提供的病历依据，与已确认选项一起由病历模型压缩为自然临床叙事。最终主诉统一采用“具体慢病 + 复诊配药”；现病史不得写患者年龄/性别、库存、推荐方案、原始历史日期或模板化“待医生核实”尾句，药品只保留规范名称，规格、包装、频次和总量留在治疗方案中。
   - 复诊药品用药天数不得由模型自由决定。每个药品优先沿用最近一次同药结构化历史处方的 `takeDays`；无法按医嘱主键、药品主键或唯一规范药名可靠关联时不得自动继承。没有历史天数或院内明确默认策略时保持为空并取消自动选中，模型返回的 `days` 只能作为非权威建议且不得进入最终处方字段；等效替换药不自动继承原药天数。
   - 复诊配药结果中的“注意事项”优先使用该场景 `healthEducation`，结果页必须将其映射到门诊病历 `outpatientRecord.precautions`，不得因字段遗漏落入普通门诊默认文案。内容只围绕规律用药、相关指标监测、已知药物不良反应和合理复诊条件；没有转诊依据时不得生成“1周内复诊”“必要时上级医院进一步治疗”等泛化要求。模型输出不合规时使用慢病安全兜底文案。

3. **结构化与智能引导问诊（AI 驱动的症状网络）**
   - **智能病历引擎**：当前开放西医症状模板链路；中医模块仍处于开发中，医生点击中医入口时只提示“开发中”，不得进入中医模板、四诊采集或中医推荐链路。西医链路继续具备“伴随症状智能推荐”和“单选/多选/跳过”等复杂逻辑判定体系。
   - **自动病历生成**：医生仅需简单勾画点击，系统会调用大模型按基层全科个人模板风格生成“主诉”和“现病史”，并由本地场景模板补齐既往史、个人史、家族史、体格检查和注意事项，优先区分急性症状、慢病复诊、检查异常、健康体检、外伤换药、外配药/处置等门诊场景；模型不可用或输出不合格时，桌面端退回本地规则草稿，保证医生仍可继续进入病历详情并编辑。

4. **大模型结构化引擎数据映射**
   - **打通自由文本与业务系统的壁垒**：为了将大模型生成的自由文本型诊断/用药建议直接应用到医院现有的生产系统（HIS）中，内置了高性能的数据匹配机制。
   - **精准字典映射**：产品能毫秒级提取大模型给出的非结构化描述，并基于 HIS 同步后的标准目录与本地缓存完成词林映射；其中药品目录必须按当前有效发药药房 scope 使用，不再以 CSV 药品数据作为兜底，最终向生产系统输出标准化的结构数据草稿。
   - **库存约束推荐**：所有可能产出药品建议的 AI 场景，在生成前都必须读取当前医生可用发药药房的有效库存目录。报告回诊等需要先判断处置方向的场景，先输出“是否需要用药 + 首选规范通用名/别名”，再只传入库存内精确命中的候选名称和规格进行处方生成；未命中时返回规范通用名作为无库存参考，不得编造其有库存。临床等效替代只允许建立在经审核的“通用名/成分—治疗类别—适应症—替代关系”知识映射上，未配置该映射时不得由模型从全量库存猜测等效药。目录不携带具体可用数量；库存命中项的名称和规格必须与院内目录一致。目录为空或暂不可用时仍必须向模型声明只能返回规范通用名。目录短时缓存以减少 HIS 压力，刷新失败可降级使用最近一次非空缓存，但医生最终提交处方前仍执行实时库存校验；校验单价按当前药房和药品从有效库存批次目录获取，不能使用药品详情单价。

5. **AI 临床决策辅助 (CDSS)**
   - **大模型私有化接入**：支持无缝对接兼容 OpenAI 协议的各类大语言模型（如 DeepSeek 等）。
   - **智能推理**：面对不典型症状，AI 能够通过解析对话上下文提供高质量的诊断鉴别建议、开药指导和检查建议。
   - **建议可编辑**：所有 AI 推荐仅作为草稿，支持编辑、删除、替换，且修改后不中断当前问诊页面状态。
   - **配置归属明确**：桌面端只保留区域后台托管运行形态。模型、独立审查 AI、语音和知识库等 API 配置由 `floating-ball-server` 统一维护，桌面端不再保存或直连任何第三方密钥。后台地址优先取构建时注入的 `VITE_REGIONAL_BASE_URL`，机构编码默认回退到 `ORG001`，如需覆盖只通过本地 `.env` 或设置页配置。设置页允许修改后台地址和机构编码，但不再提供模式开关或“模型配置”页签。保存接入参数时，即使后台暂时不可达，参数也会先保存成功，并单独提示当前连接状态；依赖后台的能力必须明确失败，不得回退为桌面端直连第三方。本地 HIS Bridge、PHIS 回写、SQLite/缓存、Tauri Store、失败重传队列和确定性本地规则继续保留，它们属于桌面基础设施而非本地运行模式。“测试 server 连通性”仅验证更新策略、注册与 bootstrap 链路，并必须在超时或不可达时结束等待态。
   - **设备身份基线**：服务端设备编码首次默认取当前机器 MAC 地址并持久化用于设备注册；只有在当前环境无法读取 MAC 时，才回退到本地兜底编码。Windows 端通过系统网卡 API 读取 MAC，不再拉起 `getmac` / `ipconfig` 等外部命令窗口。
   - **失败提示明确**：聊天入口、语音转写及其它直接依赖 AI 代理的交互，在后台未配置 AI 服务地址、密钥、模型或服务不可达时，必须立即结束等待态，并向医生展示明确错误原因；不允许出现持续转圈或仅保留空白回复气泡的状态。
   - **错误信息可理解**：医生可见错误必须说明“哪里失败、医生能做什么、必要时如何排障”。网络不可达、请求超时、后台 5xx、签名 / 鉴权失败、上游 AI 不可用、JSON 解析失败等技术异常不得原样展示为 `Load failed`、`Failed to fetch`、`Internal Server Error`、Java/SQL 类名或堆栈片段；服务端接口返回 `requestId` 时，提示中应带出请求 ID，便于后台管理员按日志定位。
   - **问题反馈闭环**：医生可在任意工作视图的顶栏直接打开反馈入口，填写 1-5 分评分、文字说明，并使用内置截图或上传现成截图。反馈提交时会自动带上最近一次 AI 调用摘要，供后台排查。
   - **设置保存可见性**：系统设置中的“通用设置”必须提供固定可见的保存入口，并明确提示未保存状态。
   - **快捷键设置归属**：应用内全局导航快捷键属于桌面端本地偏好，必须与通用设置共用保存/未保存提示；不写入患者数据、服务端配置或数据库。快捷键录入按钮应直接捕获下一组合法组合键，展示当前平台对应的 `⌘ / Ctrl / Alt / Shift` 文案，并提供单项清除与整组恢复默认；设置页上下文内的保存动作固定使用标准 `Cmd/Ctrl+S`，只在设置面板自身处理，不新增第二个全局监听器。
   - **设置入口收敛**：系统设置当前只承载“通用设置”“关于版本”，不再提供“模型配置”“数据管理”或“症状库管理”入口；基础数据缓存管理和 HIS 联调日志继续从设置页独立入口打开。
   - **内网更新闭环**：关于版本页支持配置正式/测试内网更新源，默认指向 `floating-ball-server` 公开的 `/v1/client/releases/{channel}/latest.json`；管理员在后台上传 Tauri `latest.json` 与安装包后，桌面端通过 Tauri updater 完成检查、下载、签名校验、安装和重启。内网可使用 `http://` 更新源，但仍必须通过签名校验。后台发布版本时可勾选强制更新；强制更新生效后，低于最低可用版本的桌面端只能访问更新策略、检查更新和下载安装包，不得继续使用问诊、语音、知识库、AI 代理、模板同步、反馈等业务功能。门禁页必须始终以后台强制策略为事实源：若当前版本仍低于最低要求但 updater 暂未取得安装包，只能提示“未获取到可用安装包，请联系管理员或检查更新源”，不得显示“当前已是最新版本”或解除业务阻断。

## 赋能基层医疗的核心场景

- **治疗推荐卡片单层编辑约束**：治疗推荐卡片只保留一级展开，不再叠加“更多编辑”二级层级；勾选仅负责纳入 / 移出方案，“编辑处方 / 收起编辑”仅负责编辑区开合，点击卡片空白、名称、字段或属性 chip 均不得改变勾选状态。页面同一时刻只展开一条治疗项，切换展开项时必须保留前一项已编辑内容；勾选失败时自动展开当前项并聚焦首个可编辑缺失字段，编辑区提供明确的“完成并选中”动作，已选中状态不得自动收起编辑器。药品编辑区采用统一紧凑字段网格，发药药房只在卡片头部保留一个入口；附加字段末尾顺序固定为“医保限用 → 规定病 → 备注”。本约束覆盖下文关于药品核心字段与“更多编辑”分层的旧描述。

在基层医疗机构（如乡镇卫生院、社区卫生服务中心、村镇诊所），医生往往面临**全科化挑战大、时间紧凑、病历书写不规范、上级指导缺乏**等痛点。本产品主要在以下场景深度赋能：

### 特性 1：破解“看不了、看不准”的全科诊断难题
- **场景痛点**：基层全科医生知识面广但深度受限，特别是遇到相对生僻或跨科室症状时容易漏诊误诊。
- **赋能点**：当输入核心症状时，系统会自动进行**“伴随症状智能推荐”**，提示医生沿着推荐链路进行深度问诊排查；遇到疑难杂症，一键呼叫 AI 助手给出多维度的鉴别诊断。产品充当了一台全天候的“规培带教机器”和专家智库。

### 特性 2：拯救“耗时、易错”的病历书写泥潭
- **场景痛点**：每天面对庞大的就诊量，手敲病历耗费大量精力，且主诉和现病史的格式经常被质控部门打回。
- **赋能点**：通过“点选式”的高效问诊收集，智能文本引擎根据基层门诊模板风格和医学书写规范，生成可编辑的整张门诊病历草稿，包括主诉、现病史、既往史、个人史、家族史、体格检查和注意事项，并自动压缩无效问卷项、保留症状各自时间线和关键阴性。医生既可按完整链路一次生成，也可在医生站内按需触发生成并直接回写 HIS 草稿，将原本 3 分钟的书写时间压缩至 10 秒，腾出更多精力用于医患沟通。

### 特性 3：不打断门诊节奏的灵活辅助
- **场景痛点**：基层门诊接诊节奏快，医生常常只需要某一类建议，而非被迫走完整套 AI 流程。
  - **赋能点**：浮舱改为“同一问诊页面内灵活模式”的方式记录本次就诊辅助。医生既可从症状采集开始，也可直接带着 PHIS 草稿进入病历详情页，自动生成推荐诊断，并继续在同一页面完成诊断鉴别、推荐用药、推荐检查与病历回写。对于只需要一张处置清单的场景，HIS 可触发独立“诊疗方案推荐”页，基于当前主诉、现病史与诊断聚合用药、检查、检验、处置四类建议，医生勾选后仍按统一 `record-confirmed` 回写。对于院内已有检验/检查报告的二次解释需求，则通过独立报告解读窗快速给出辅助判断，不占用问诊主链路。
- **可理解性要求**：患者基础信息只作为原始上下文展示；AI 生成的主诉、现病史必须和 PHIS 原始字段明确区分；若 PHIS 已经有主诉、现病史，系统应直接用这些内容作为当前页面左侧病历详情的初始值。
- **语音问诊结果页可读性要求**：病例正文（主诉、现病史、既往史、个人史、家族史、体格检查、注意事项）优先级高于推荐依据说明；语音问诊结果页应优先保证医生一眼看到本次完整病例内容。右侧推荐区默认只展示“推荐结论 + 关键操作态”，长段“依据分析/主要依据/模型说明”默认折叠，医生按需主动展开查看。
- **患者基础栏过敏史展示要求**：智能问诊、语音问诊和独立诊疗方案等复用患者基础栏的页面，在接诊上下文存在明确过敏史时必须直接展示“过敏史：具体内容”，不得只展示泛化的“过敏史”标签；长内容可在基础栏内截断，但必须保留悬停查看完整过敏内容。
- **问诊结果页统一约束**：语音问诊和症状问诊进入结果编辑阶段后，必须共用同一套结果页组件与交互语义；两条链路只允许“问诊渠道 / 初始数据来源”不同，不允许长期并行维护两套结果页结构。共享诊断卡必须展示诊断置信度/匹配度（`Diagnosis.rate`）和推荐依据入口，语音问诊与智能问诊结果页都必须由共享结果页直接提供同款“诊断鉴别”入口和 checklist 弹窗，不再由 `ConsultationPage.vue` 维护独立调试弹框。诊断鉴别 checklist 必须同时覆盖两类情况：一是当前诊断与主诉、现病史明显不匹配或不能解释主要症状时，提示医生复核诊断方向，并按严重复核状态展示，默认展开完整错误内容，不得混入普通排查清单或空态；二是当前诊断虽基本成立但存在高危疾病混淆风险时，提示关键问诊或查体排除项。只有诊断与病历信息基本一致且无明显高危排查需求时，才允许返回空 checklist。症状问诊结果页必须保留底部“返回”入口，点击后只回到症状问诊编辑页继续补充，不触发放弃确认或清空当前结果；底部“放弃”则必须经过二次确认，确认后清空当前智能问诊快照和页面内勾选/推荐数据，并直接缩小回悬浮球，下次进入重新开始智能问诊。若症状问诊现有字段、编辑粒度、推荐项结构与语音结果页不一致，以语音结果页当前标准为准。
- **一键回写诊断安全约束**：症状问诊和语音问诊的一键回写都必须只把 PHIS 标准诊断库 ID 写入 `diagList.idDiag`，该值对应 PHIS `ID_DIE`；AI 自由文本、前端临时 key、PHIS 草稿诊断文本在未命中标准诊断库时只能用于展示和医生判断，不得作为可保存诊断提交。症状问诊切入共享结果页时必须保留已匹配的标准诊断 ID，未匹配诊断在提交前要明确拦截并提示医生先切换或重新匹配标准诊断。
- **一键回写成功收尾约束**：症状问诊和语音问诊一键回写发出 `record-confirmed` 后必须进入等待 HIS 回执状态；收到匹配 `requestId` 的成功回执后提交最终日志并弹出整页反馈界面，医生提交反馈或点击“暂不反馈”后再收起结果页回到桌面待命。失败回执必须保留当前编辑现场、展示失败原因并允许医生修正后重试。
- **完整门诊病历回写约束**：一键回写时通过 `record-confirmed.outpatientRecord` 携带完整门诊病历字段。该对象只包含主诉、现病史、既往史、个人史、家族史、体格检查和注意事项，不包含 `diagnosisText`；病历诊断行由 HIS 根据 `diagList` 自动生成。体格检查优先使用 HIS 已传入生命体征或医生确认后的模板，不得在缺少依据时把默认生命体征伪装成实测结果。
- **一键回写医嘱非空安全约束**：用药、检查、检验、处置在勾选或最终一键回写前必须通过同一套必要字段校验。所有医嘱都必须有标准服务 ID、服务名称、服务分类编码、执行位置和医保限用；药品还必须有一次剂量、剂量单位、频次、用法、总量、天数和发药药房；检查必须有检查部位；检验必须有非空附加 `jsonField`；处置必须有大于 0 的数量。药品一次剂量、总量、天数和处置数量必须是有效正数；一次剂量整数最多 4 位、小数最多 4 位，总量整数最多 4 位、小数最多 2 位，用药天数必须为 1-365 的整数。必填字段必须在编辑区用传统表单方式明确标识，当前值为空、格式非法、超出长度或被医生清空时输入框飘红并禁止勾选 / 一键回写，提示医生补齐或重新匹配标准库，不得把非法字段写入 `orderList`。医生手动清空检查 / 检验 / 处置的执行科室，或清空任一医嘱的医保限用后，当前输入框空值就是最终状态，页面必须以红框提示并禁止选中或一键回写，不得再用标准库匹配项、详情 hydrate、默认医保类型或默认科室兜底通过校验。药品、检查、检验、处置的备注字段必须限制在 200 字以内；前端应展示当前字数与超限提示，并在勾选 / 一键回写前拦截超长备注，不得在 payload 构造时自动裁剪医生输入。
- **药品推荐定稿安全约束**：语音问诊、症状问诊、报告回诊、慢病复诊和独立诊疗方案产生的药品，在展示为可选草稿、自动选中、库存校验、缓存或回写前，必须经过同一条定稿流水线：当前药房有效库存目录对齐 → 标准药品匹配 → 按候选药房加载药品详情 → `targetDose/targetDoseUnit` 换算为 PHIS `doseOnce/unitDose` → 频次和用法映射为 HIS 标准编码 → 根据最终一次剂量、单制剂含量、频次、天数和包装因子计算总量 → 按当前药房与药品从有效库存目录取得单价 → 使用最终包装总量校验实时库存。模型只输出临床目标剂量、频次、用法和疗程建议，不得输出或决定包装总量；模型返回的 `totalQty/totalUnit` 一律不作为药品处方事实。对于“必要时”等已匹配 HIS 标准字典、但无法折算固定每日次数的频次，在一次剂量、剂量单位、天数和药品销售包装单位均完整时，程序总量统一兜底为 `1 × 销售包装单位`，并展示“无法精确换算、请医生确认”的说明；该兜底只解决包装总量，不得补造一次剂量、频次、用法或天数。库存合并项存在多批次价格时使用近效期有效批次价格，该批次价格无效时顺延；不得使用药品详情单价兜底。任一环节缺少库存同品、药品详情、标准频次 / 用法、可用的精确或单包装兜底总量、有效库存单价或库存充足结果时，药品不得自动选中或回写。医生手动修改一次剂量或总量后以医生输入为准，但必须重新计算 / 校验依赖字段和库存，后台 hydrate 不得覆盖医生已确认的一次剂量。
- **标准项目详情反填约束**：AI 推荐检查 / 检验 / 处置时不得臆造执行科室；标准库匹配成功、候选确认或手动匹配后，必须通过 HIS 诊疗项目详情反填真实执行科室、单位和处置默认数量。执行科室只允许来自医生当前选择或详情接口返回的标准执行科室 ID；处置数量只在当前为空且医生未手动修改时，使用详情接口返回的默认数量。若详情缺失或未返回对应字段，页面继续按必填校验拦截医生补齐，不得以“治疗室”等固定值兜底。医生手动清空字段时，当前空值就是最终状态，后续 hydrate 不得自动补回。
- **共享结果页适配约束**：症状问诊和语音问诊进入结果编辑页前必须先转换成中性 `ClinicalResultInput`，再由结果页内部适配到当前 UI 所需结构。适配层必须完整保留上游已确认的 `matchedItem`、`suggestedMatchItem`、`matchStatus`、`manualMatched`、`selected`、发药药房、执行科室、检查部位、频次/用法 key、总量和医生编辑痕迹；只有上游缺少标准库匹配信息时，结果页才允许按当前目录重新评估匹配，不能覆盖医生已确认的数据。患者 / 就诊锚点变化是硬隔离边界，结果页必须先清空上一患者的诊断选择、治疗方案、反馈草稿和等待回写状态，再应用新患者结果；当前患者无诊断或新结果尚未加载治疗方案时，只能展示空治疗态和刷新入口，不得显示上一患者的“已切换主诊断，当前方案仍保留上一版”状态。
- **智能问诊 AI 鲁棒性约束**：智能问诊的诊断、用药、检查、检验、处置等结构化 AI 调用必须向语音问诊结果页看齐：请求发起时不得先清空已有可用结果，只有在新响应成功解析并确认属于当前诊断上下文后才能覆盖；JSON 解析必须允许模型返回代码块或少量前后说明，并从响应中提取第一个完整 JSON 对象/数组；单路推荐失败时保留上一版结果并展示明确错误，不得让医生看到整页空结果；慢请求返回时若医生已切换到其他诊断，必须丢弃旧响应，避免覆盖新状态。共享结果页的“诊断建议”区必须提供医生手动触发的“刷新诊断”入口，允许基于当前病历重新进行 AI 诊断推荐；刷新期间保留上一版可用诊断，诊断区展示与治疗方案一致的内嵌 loading 占位，成功后再替换为新推荐，并按新主诊断提示医生手动刷新治疗方案。
- **症状问诊结果页病例补全约束**：症状问诊切入共享结果页时，既往史不得默认为空字符串。若 HIS / 接诊上下文已提供明确既往史，则直接使用；若缺少显式既往史但存在患者基础信息与历史就诊记录，应优先生成“既往门诊记录”式摘要作为初始既往史，供医生在结果页继续确认和编辑。
- **语音问诊结果页紧凑约束**：左侧病历正文区只保留必要宽度，右侧推荐区优先展示诊断/药品名称、规格和核心操作态；诊断区不再额外展示主诉/现病史概览面板，直接以标题明确“诊断建议”；诊断卡必须优先保证诊断名称可读，置信度、编码、主诊断标记和操作按钮应作为稳定右侧扫描区存在，只有容器确实不足时才整体换行，不得让置信度标签压住或过早挤断诊断名；治疗方案内的药品、检查、检验既然已经按分组展示，卡片标题行不得重复追加同类类型标签；名称、规格、匹配状态等辅助信息应尽量压缩在同一行展示，避免为辅助说明单独占行；依据说明改为紧贴诊断名、药品名、项目名后的图标提示，鼠标悬停和点击都应能看到完整内容；药品在未展开更多编辑时，也应直接在卡片头部展示“一次剂量、频次、用法、天数、总量”的文本摘要，减少医生额外点开成本；药品医嘱默认仅展开“一次剂量、频次、用法、总量”四类核心字段，其余字段收进头部箭头触发的“更多编辑”；这四类核心字段默认以可阅读的文本态呈现，医生点击后才进入编辑态，其中频次、用法必须保持可点击、可搜索、可修改，不得用置灰样式暗示禁用；规格、剂量单位、总量单位应优先展示模型返回值并保持只读，不再提供下拉编辑；药房、执行科室、医保限用等枚举字段在语音结果页应复用频次/用法同款的可搜索选择浮层，不再使用浏览器原生下拉框；这类可搜索选择器在已有值时再次打开，默认仍应展示完整候选列表，而不是被当前值锁定过滤；已有值的可搜索输入框右侧应提供小号清空按钮，候选列表只展示候选项；当发药药房存在多个有效候选时，药品项确认匹配或选中前必须按药房顺序轮询 `medicineDetail`，以第一个返回有效药品信息的药房作为该药品默认药房；若所有候选药房均无有效药品信息，必须提示医生且禁止选中该药品；药品匹配后，医生修改用药总量时必须调用 `api/phis.medicineInventoryService/checkInvEnough` 校验当前默认药房库存，库存不足提醒必须前置到“确认匹配 / 勾选 / 修改药房或总量”等单药操作当下，并明确带出具体药品名；提交前的库存校验仅作兜底，不应成为医生首次得知库存不足的时点；检查和检验的执行科室必须在卡片头部直观展示，并支持点击后直接打开可搜索选择器调整；执行科室为空时必须提示医生先设置，且禁止选中该检查/检验项目；执行科室已设置后，卡片头部只展示具体科室名称，不再重复显示“执行科室”标签；检查和检验的附加属性区中，执行科室应置于首位，医保限用后再展示规定病，备注放在最后；检查项目基础数据匹配成功后，必须通过 `api/phis.hiBdCliPacsPartService/queryExaPartAndWayList` 获取检查部位 / 方式候选；候选只有一项时默认选中，多项时在附加属性区提供可搜索选择器由医生调整；部位字段仅保留给检查项目，检验项目不再展示；检查与检验的总量字段不再给医生编辑，默认按 1 回写；发药药房、执行科室、检查部位与库存门禁必须覆盖语音问诊、智能问诊共享结果页、完整问诊遗留回写路径和独立诊疗方案页，任何入口不得绕过。当前已选中的诊断卡再次点击，或点击其推荐依据、反馈、同类诊断下拉、诊断鉴别等内部操作按钮时，不得被当作重新选择诊断，也不得触发治疗方案刷新；医生主动切到另一条诊断后，页面应保留现有方案并提示可手动点击“刷新方案”获取当前诊断方案；医生手动刷新后返回的结果也应尽量保持规格、一次剂量、频次、天数、总量等结构化字段，不得长期退化为仅有 usage 大文本；处置、检查、检验默认不展示编辑项，仅在医生主动展开时显示；当前页诊断项只允许单选切换，不允许点击后取消为空，也不再触发诊断鉴别排查弹窗。
- **语音问诊标准库匹配安全约束**：药品与诊疗项目（检查 / 检验 / 处置）必须采用保守匹配策略。只有名称完全匹配标准库时，才允许直接显示“匹配成功”并参与默认选中；仅达到高相似度的候选项应直接表现为“候选待确认”语义，并把“确认匹配”操作紧贴候选标准项展示，必须由医生显式点击确认后才可纳入回写；这类候选项提示区应以浅底色块和清晰边界突出显示，按钮样式需与提示区保持同一视觉语义，避免像独立悬浮操作；未命中标准库的项默认保持未匹配状态，不允许直接选中，医生只能通过“手动匹配”从标准库候选中明确选择后再纳入。
- **药品目录与发药药房约束**：药品标准库匹配不得再使用 CSV 药品数据兜底，只允许使用当前有效发药药房对应的 HIS 药品目录。发药药房下拉优先使用 `fetchAvailablePharmacies()` 返回的有效药房；若该接口无可用数据，才允许使用 `fetchMedicineStoreIds()` 返回的药房 `idSto` 作为兜底候选。自动匹配到药品后，发药药房候选必须收窄为该药品目录实际所在药房；未自动匹配时，手动标准库候选应来自全部当前可用药房对应的药品目录，医生手动匹配后再按所选药品的 `storeIds` 收窄药房。
- **语音问诊异步复核员约束**：语音生成病历和诊疗建议后，应异步启动安全复核，不阻塞医生阅读和编辑当前结果。复核员以“非干扰提醒”方式呈现，只提示可能影响首道安全底线的问题，例如危险信号遗漏、过敏/既往史冲突、当前建议与近期用药或病情信息的潜在风险；提醒文案必须可操作，优先给出“复查心电图 / 补问症状 / 考虑替代 / 关注禁忌”等临床动作，不直接替医生做处方决策。高危提醒在确认提交前需要医生知晓，但默认不强制拦截；中低危提醒不得打断主流程。当前阶段先基于语音结果、患者基础信息、既往史/过敏等本地上下文复核，后续接入 HIS 历史用药时必须通过独立安全上下文服务，不得把 HIS 查询逻辑塞回结果页。
- **语音问诊结果页视觉基线**：全页最小字号不得低于 13px；常规正文、标签、输入值默认使用 14px；重要信息如诊断名、药品名、核心字段值统一使用 14px 且加粗；选中态只允许使用贴左上角边框的轻量勾选标记，默认采用“淡底 + 深色图标”，不得再依赖整块背景色或高对比边框提示；语音问诊结果页的背景、卡片、按钮、标签和浮层应跟随系统配置中的风格设置自动换肤，不得长期写死为单一蓝白主题；整体氛围应以“上亮下稳”的轻层次背景为主，避免大面积自下而上的渐变或过重色块影响医生阅读；放弃和一键回写操作固定放在患者基础信息栏右侧，其中“放弃”需先经过二次确认，再清空当前患者未提交的语音缓存并直接退回小球状态；内容超出窗口高度时必须由结果页自身提供纵向滚动，不能裁切正文或推荐区；右侧推荐列的最外层容器必须由组件自身控制圆角和裁切，避免被全局面板样式打出尖角。
- **语音问诊反馈闭环约束**：语音结果页必须支持对单条诊断 / 治疗推荐做项级反馈，也必须支持对整页结果做 1-5 分整体评分与点评；项级反馈至少提供“有用 / 不满意 / 已修正采用”三类动作，其中“不满意”必须记录结构化问题标签与文字原因，“已修正采用”必须允许保留医生最终采用结果；病例详情中的主诉、现病史、既往史也必须提供字段级反馈入口，默认贴在字段标题旁，不得挤占正文阅读区域；若医生手动修改过字段，反馈数据中必须同时保留 AI 原文、医生当前值和可读的差异摘要，便于后续复盘具体改动；整页反馈不得常驻占用语音结果主界面，应在一键回写收到 HIS 成功回执后再以浮层方式弹出，并允许医生直接跳过关闭；该浮层不能影响回写成功本身，也不应把反馈填写前置为主流程门槛，但在低分场景下至少要求补充一个问题标签；一键回写成功只表示本次 HIS 回写闭环成功，不等同于语音问诊结束，同一就诊再次触发语音问诊入口时仍应恢复上一张语音结果页和编辑数据；所有反馈都应自动绑定当前会话、最近一次 AI 调用摘要、病例上下文摘要与推荐快照，先完成桌面端本地持久化，再由后续后台接口按标准 payload 推送。
- **交互约束**：主诉/现病史支持单独回写电子病历；诊断鉴别的确认只记日志、不修改病历；HIS 通过 `assist.action = diffDx` 单独触发鉴别诊断时，应直接打开独立“鉴别排查确认”小窗并开始生成建议，不进入后续问诊结果页 / 工作站页面；诊断鉴别请求超时、网络失败、解析异常或客户端运行时错误必须显示为中性的“生成失败”状态并提供重试，不得计入“发现问题”数量、不得附加“高风险”徽标，也不得与成功解析出的临床风险共用同一列表；推荐诊断、推荐用药、推荐检查需要通过“引用”进入 PHIS 保存闭环，PHIS 保存成功或失败后再把结果回执给桌面端；症状问诊记录页底部的“一键回写”必须与语音问诊保持同一语义，直接触发最终 `complete_consultation` 结果写回，但结果页不能在请求发出后立刻结束，必须等待 HIS 通过 `reference-feedback` 回执最终成功/失败；失败时页面保持打开并展示错误原因，成功后才允许进入成功收口；PHIS 批量引用仍保留为独立能力，但症状问诊共享结果页当前阶段暂不展示单独“批量引用 PHIS”按钮，避免与最终提交形成双主操作；与第三方 HIS / PHIS 联调相关的本地 Bridge 入站请求和 PHIS 出站请求必须生成可检索、可导出、已脱敏的调用日志，并用 `traceId` 串联请求、响应和错误；推荐依据图标点击展开后，点击其它区域或触发失焦时应自动收起，不得要求医生再次点击同一图标关闭。
- **一般情况问诊“其他”补充约束**：一般情况问诊中的精神、睡眠、食欲、二便、体重等单选项一旦选中“其他”，当前行必须立即出现补充输入框，允许医生填写详情；若已选“其他”但未填写补充说明，则不得继续生成病历；生成的现病史文案必须优先写入该补充说明，而不是只保留笼统的“其他”。
- **症状问诊推荐区视觉基线**：症状问诊记录页右侧的推荐方案应优先采用“医嘱清单式 worklist”视觉，而不是内容卡片流。推荐用药 / 检查 / 检验分组标题仅作为小节标题存在，默认不承担强装饰；组内每条推荐项应首先呈现“名称 / 规格 / 匹配状态 / 必填属性 / 核心操作”这一条稳定扫描路径，减少不同项之间的布局跳动；推荐依据、AI 原建议、候选标准项、处方摘要等补充信息允许压缩在主行下方，但不得破坏清单阅读节奏；执行科室 / 发药药房等必填属性应作为稳定属性列存在，避免与标题说明文字混排；当推荐项名称、匹配状态、必填属性和操作按钮同时出现时，右侧状态区必须允许上下两排布局，优先保证药品 / 项目名称与摘要文案不被状态标签或按钮覆盖，且第二排状态 / 必填属性在空间足够时应保持右对齐；医生确认勾选后才展开二级编辑区，避免默认把所有依据与编辑字段铺满一屏。
- **引用入口约束**：推荐诊断保持单选，并可在当前选中诊断上直接发起引用；推荐用药、推荐检查检验支持多选后按分组一次引入，不应退化成逐条重复点击引用。暂不支持 PHIS 引用的处置建议应明确标注为“仅展示、不可引用”。
- **独立诊疗方案推荐约束**：`assist.action = treatment_plan` 承载独立聚合方案页，只复用当前接诊上下文中的主诉、现病史、既往史、过敏史和诊断，不启动完整症状问诊。页面按推荐药品、推荐检查、推荐检验、推荐处置四组展示，默认以清单勾选为主，不暴露完整病历编辑器；推荐区标题只展示“推荐方案”，不在标题后追加诊断名说明，避免医生误解为推荐来源只取自某个标题诊断；但每条推荐项必须复用语音问诊 / 智能问诊共享结果页的二次编辑能力，允许医生继续调整药品的一次剂量、频次、用法、总量、天数、发药药房、医保限用、规定病和备注，以及检查 / 检验 / 处置的执行科室、检查部位、医保限用、规定病 and 备注。检查 / 检验数量固定按 1 回写，不在页面展示数量编辑入口，也不允许医生修改；处置项目必须优先按 HIS 项目详情反填的默认数量或医生确认后的总量回写，缺失时拦截医生补齐，不以 1 兜底。药品、检查、检验、处置的标准库匹配、候选确认、手动匹配、药品详情 hydrate、执行科室 / 检查部位 / 发药药房门禁和库存校验必须复用语音问诊同一套共享能力；AI 返回的 `targetDose/targetDoseUnit` 表示临床一次剂量，匹配药品详情后必须换算为 PHIS `doseOnce/unitDose` 的同单位数值（如 `250mg -> 0.25g`），不得把“每次几个制剂单位”的比值直接拼接成 `1g/1mg`。片、粒、袋等制剂数只用于总量计算和解释；模型误把 mg/g/ml 临床剂量放入 `dosage/dosageUnit` 时，前端须先迁移到目标剂量字段再执行同一换算。目标剂量不可用时才使用 HIS 默认一次剂量，不得让模型自由文本覆盖已确定的 HIS 剂量单位；一键回写必须复用 `record-confirmed` 的 `diagList/orderList` 构造器，等待 PHIS 通过 `/reference-feedback` 回执后再展示最终成功或失败。
- **住院病历历史门诊引用约束**：入院记录补充要点弹窗中的历史门诊卡片只展示科室、日期和主诊断，不重复展示主诉摘要；主诉、现病史等正文事实以“查看”打开的门诊病历 HTML 内容为准，避免列表卡片出现“无主诉记录”等占位文案误导医生。所有来自 HIS 的门诊正文和入口携带的住院模板必须先经过统一 HTML 白名单净化后才能解析或展示：脚本、事件属性、表单、嵌入内容、危险链接和不受控样式必须移除，净化失败时回退为转义后的纯文本提示，页面不得直接渲染原始 HTML。门诊历史默认展示近 7 天，允许医生切换近 1 月、近 3 月；列表只展示同时具备有效诊断和门诊病历文书的就诊记录，无诊断或无可用病历文书的记录不进入可选列表。医生引用带正文的门诊病历后，门诊病历与手工/语音补充要点应共同作为入院记录书写依据；初次自动生成时，若既无补充要点、也无可用门诊病历正文，才停留在等待输入状态并打开补充弹窗。医生在补充弹窗中点击“直接重新生成”时，视为已确认仅基于住院聚合上下文继续生成，不得再次停在等待补充依据状态。AI 生成必须以入院记录书写角度综合门诊病历、补充信息和住院上下文重新归纳主诉、现病史、入院情况等字段，不得把门诊主诉、现病史或门诊正文原样搬入入院记录。病历预览中的页眉标题等非 AI 模板字段必须保留 HIS 模板原始默认值，不得统一替换为“病程记录”等固定文案。生成后页面必须让医生看见本次使用了哪些依据（住院上下文、门诊正文、补充要点、模板缓存）和关键耗时；医生在预览中确认并点击一键回写后直接发送回写事件（回写前质控暂不启用）。住院病历页必须区分“收缩”和“放弃”：收缩到小球只保留当前生成现场并提供恢复入口，不清空请求、预览、编辑内容或回写状态；最小化状态下再次收到同一 `admissionId` 的病历生成请求时，只恢复当前现场，不重新启动 AI 生成；“放弃”需二次确认，确认后清空本次住院病历生成现场并退回小球。
- **闭环约束**：点击引用后，桌面端应保持当前问诊窗口展开，允许医生继续查看和补充本次问诊内容；PHIS 保存完成后再通过本地回执接口返回成功/失败原因，并同步更新当前页面状态。当前实现仍属于同一运行期内的内存保活，不代表重启应用后仍能自动恢复。
- **悬浮球门禁与最小化语义**：未接诊患者或当前患者不存在未结束问诊时，悬浮球的问诊按钮必须保持可见但禁用，不得借此主动新开症状问诊或语音问诊；语音问诊只能由 HIS / SDK 推送的 `start-voice-consultation` 触发，且患者上下文缺失时立即拒绝。症状问诊和语音问诊点击顶栏收起或关闭时，必须先统一回到接待胶囊（风险提示页）；接待胶囊的收起落点尺寸以风险胶囊基线为唯一标准，HIS 直接进入胶囊和从问诊界面收起到胶囊不得出现两套尺寸。医生从接待胶囊再次退出到悬浮球时，才按"最小化"处理：原问诊界面的现场状态保留在内存中，对应的悬浮球恢复入口（症状问诊为环菜单按钮，语音问诊为双击小球）继续保持"恢复"语义，可直接还原回原界面而不重新初始化；同一医生可同时存在症状和语音两路最小化会话，互不影响，且从接待胶囊退出时不得覆写原有最小化类型；大面板收起到胶囊时，只允许切换外层视图和窗口尺寸，不得复位 `ConsultationPage` 内部页签或清空症状问诊数据，避免医生回到问诊时从结果页/编辑页被重置到症状选择页。症状问诊的页面留存必须与语音问诊一致：未诊毕、未确认放弃时，再次点击“智能问诊”或最小化后再次打开，都应恢复到上次停留的内部页面（症状采集、病历详情或最终报告）和当时的数据；智能问诊确认放弃后必须立即清除当前数据并退回悬浮球，不得回到症状界面继续保留旧勾选；患者 / 就诊锚点切换时，智能问诊必须清空上一患者的症状选择、诊断、治疗方案、缓存快照和未完成 AI 请求；若新患者无诊断，重新触发智能问诊只能生成新的诊断和治疗方案，不得继承上一患者的诊断或治疗推荐。语音问诊在一键回写成功后仍属于未诊毕现场，同一就诊再次点击语音问诊必须恢复上一张数据界面，不得重新进入录音问诊；但一旦当前接诊上下文从患者 A 切换到患者 B，语音问诊也必须和智能问诊一致，把患者 A 的未结束语音现场视为已失效并清理，之后再切回患者 A 也应重新开始语音问诊；只有诊毕、确认放弃、患者切换或跨自然日失效时才清除快照。最小化记录以就诊 ID（`idVis`）为锚点，跨自然日自动失效；恢复时使用医生上次手动调整过的窗口尺寸（沿用 `useWindowManagement` 已有的视图级持久化）。
- **语音问诊整张病历的缓存恢复**：同一就诊从缓存恢复语音问诊时，必须复用上次落到编辑器里的整张病历快照（主诉、现病史、既往史、诊断、诊疗方案、当前选中诊断、上一次 fetchAITreatment 对应的诊断键）。除非缓存丢失或跨自然日已失效，否则不得在恢复时再次触发 `fetchAITreatment`，避免医生看到诊疗方案"被重新刷一遍"。`useVoiceConsultation` 的 `editorSnapshot` 是该快照的唯一落点；编辑器内任何字段变化都按节流写回，fetchAITreatment 完成后立即写回。

## 两慢病插件（高血压与 2 型糖尿病）

- 接诊后的风险胶囊升级为“悬浮球 → 患者胶囊 → 慢病详情”的三态插件；慢病详情仍是窄幅浮层，不替代 HIS，也不演变为新的综合工作台。
- HIS 主动唤起两慢病详情必须使用独立的 `openChronicDisease(patient)` / `POST /api/chronic-disease/open` 契约，并直接进入慢病详情展开态；`sendRisks(patient, risks)` / `POST /api/patient/risks` 只处理患者风险评估，不得再作为两慢病入口复用。两慢病入口不接收 `risks`，也不触发 LLM 风险分析，避免风险提醒与慢病管理出现预期外串场。
- 两慢病详情进入真实业务链路时，桌面端先按当前患者 `idPi` 补全患者信息；取得患者 `idCard` 后，和其他 HIS 调用一致请求 Adapter 发布的 `api/phis.aiAdapterService/queryPatientVisitHistoryData`，请求体固定为 `[{"idCard":"身份证号"}]`。该 Adapter 操作适配后端 `chis.hyVisitService/queryPatientVisitHistoryData`。响应中的 `idPhr / naPi / sdSexText / ageText / pastMedicalHistory / diagnosis / chiefComplaint / rqflStatus / pressureList / pressureHist（实际返回示例亦出现 pressureHList）/ gluList / visitInfos / drugList / idRecord` 均按真实接口原字段强类型接收和消费，不得先改名成 `patientId / visitId / name / currentVitalSigns / hisHistory / medications` 等平台自定义业务结构再交给慢病插件。
- 慢病详情展示真实患者事实、公卫/临床来源标签、血压和血糖趋势、AI 推荐、慢病复诊配药入口与已发布诊疗路径。四个折叠区固定为“1 就诊历史信息、2 AI 推荐、3 慢病复诊配药、4 临床诊疗建议”；AI 推荐区只允许从当前 HIS 用户、机构与执行科室上下文实际可开立的检查/检验目录中选择项目，不得继续展示无法对应院内标准项目的硬编码建议；目录查询或推荐失败时展示明确错误和重试入口，目录中没有合适项目时展示成功空态。第 3 区只承接既有复诊候选、动态确认、处方定稿和回写链路，不在慢病插件内复制配药规则或直接生成处方。没有候选时保留该区并展示不可操作空态，缺少其他有效记录时同样展示空态，不生成示例曲线或推断签约事实。
- 只有上游明确返回高血压或 2 型糖尿病公卫管理身份的对象可进入正式慢病随访；可用态为橙色，仅有临床诊断时显示“临床识别”但随访置灰。随访采用一个融合表单，顶部只读展示上游传入的高血压 / 糖尿病标记：`rqflStatus=3` 映射 `sdVisitKind=["1"]`，`rqflStatus=6` 映射 `sdVisitKind=["2"]`，`rqflStatus=3,6` 映射 `sdVisitKind=["1","2"]` 并显示两类字段并集，不再通过病种 Tab 切换。字段名、条件显示、校验和保存结构以对接系统原始 `TcdVisitForm` 实例为准；查询返回的 `idPhr / idRecord` 直接进入表单，其中 `idRecord` 必填且不得由入口 `idVis` 代替。人员、登记表、医生和药品等仅用于系统关联且不需要医生操作的标识字段必须由接诊上下文、登录身份、历史记录或标准药品匹配自动带入，医生界面不得展示或提供手工编辑入口；缺失时应提示重新进入随访或核对上游信息，不得要求医生填写技术标识。医生界面的其他字段标题、占位提示和校验信息统一使用中文业务名称，不展示接口字段 key、字典标识或模板内部名称；这一展示约束不得引入字段翻译层，内部表单和出站请求仍直接使用真实接口原字段。提交时补齐原字段 `advBmi`，多选数组按原实例转成逗号字符串，联合随访一次点击只构造一条记录并以 `[TcdVisitForm]` 调用 Adapter 发布的 `api/phis.aiAdapterService/saveTcdForm`。该 Adapter 操作适配后端 `chis.tcdService/saveTcdForm`；客户端不再把随访保存转发到区域后端，也不构造自定义慢病保存 DTO 或自定义成功响应。
- 诊疗路径、健康处方与年度评估在独立原生窗口打开。高血压与 2 型糖尿病路径必须复用原两慢病系统的完整正式流程图，不得用线性节点列表或患者证据状态自行改写临床流程；高血压图保留原“无合并症药物治疗、生活方式干预、有合并症治疗方案、合并疾病降脂目标”热点及只读路径说明抽屉，糖尿病图按原图只读展示。插件勾选的检查检验必须进入现有诊疗方案工作台完成标准库匹配和医生确认，不得用提示文案冒充医嘱草稿；健康处方由 AI 生成检查检验、用药复核与生活方式草稿，必须由医生逐项确认后在桌面端本地打印。年度评估先按所选自然年确定性聚合血压、血糖和有日期的用药事实，再通过现有签名 `/v1/ai/chat` 生成六类医生评估草稿：年度控制与趋势、心血管风险与共病、并发症/靶器官筛查、用药与安全性、生活方式与自我管理、随访重点；缺失的糖化血红蛋白、肾功能/尿白蛋白、血脂、眼底、足部、吸烟饮酒、饮食运动、依从性和不良反应等信息只能标为“待核实”，不得推断为正常或已完成。AI 不得自动诊断、调整药物、确定个体控制目标或改变临床规则；医生确认后方可本地打印。健康处方和年度评估均不向区域后端保存慢病快照。
- 插件 AI 推荐必须复用既有 HIS 检查/检验可开立目录和签名 `/v1/ai/chat`，模型只返回目录 `catalogRef`，页面再展示真实标准项目名称。医生在插件内勾选后，客户端先以所选项目调用 `ClinicDoctorCoreService.loadVisCliList` 加载诊中助手调入映射，再把映射后的 `matchedItem` 和当前 `idVis` 绑定到现有诊疗方案工作台，继续完成执行科室、检查部位等必填属性确认；最终确认只对这一慢病来源调用 `ClinicDoctorCoreService.saveOdsImp`，检查/检验统一进入 `applyVOS`。返回 `200` 才表示 HIS 已保存；返回 `401` 时必须展示服务端 `msg`，医生确认继续后以同一请求仅把 `forceSave` 改为 `1` 再次提交；返回 `500` 时保留工作台并展示失败原因。该分支不得再并行产生 `record-confirmed`，避免重复保存；其他诊疗方案入口仍保持既有 `record-confirmed + /reference-feedback` 闭环。接诊插件本身只负责选择和进入确认工作台，不直接保存检查/检验项目。
- 患者或就诊锚点切换时立即关闭旧患者慢病业务窗；复用窗口收到新 payload 时必须重新挂载业务页面，旧表单不得进入新患者请求。
- 患者胶囊与慢病详情统一使用 3 号视觉基线中的 3D 医疗 AI 机器人形象，不复用旧版扁平圆形机器人头像。
- 慢病详情采用原始窄栏插件版式：顶部合并患者身份、接诊状态和签约管理信息，3D 机器人悬跨在内容卡右上角；四个折叠区与底部三个业务入口统一收在单个圆角内容卡内，底部入口在内容滚动时保持可见。折叠区标题只保留蓝色序号、标题、摘要和展开箭头，不增加与原设计无关的装饰图标；复诊配药展开内容沿用当前慢病紫色卡片语言，并通过医生确认后才进入既有配药流程。正式界面只显示任务本身、必要状态和可行动信息，不在操作卡片下方常驻解释实现流程或设计边界的辅助小字；安全边界由“待确认”状态、确认页面和回写门禁体现。
- 一期不包含患者端，也不提供医生或 AI 自由配置医学规则的页面。详细交互、事实来源与安全边界见 [两慢病插件设计与实现基线](./docs/two-chronic-disease-plugin.md)。
