# 智医助理 (MedHermes) —— 基层医疗 AI 辅助工作站

**智医助理** 是一款专为基层医生量身定做的桌面端 AI 医疗辅助工作站。产品突破了传统 HIS 系统的封闭和繁琐，以“无边框透明悬浮球”的形式静默运行于桌面，不干扰医生现有的任何工作流。

产品坚持“医生主导、AI 辅助”原则，保留原有症状问诊主流程作为默认入口，同时将原灵活模式入口收敛进同一条智能问诊链路。医生既可以从症状采集开始完整问诊，也可以在已接诊患者上下文中，带着 PHIS 已有的主诉、现病史和诊断信息直接跳入病历详情页、推荐诊断页、诊断鉴别页或用药/检查页；对于检验检查报告解读这类不应打断当前问诊编辑的独立分析任务，则允许以单独结果窗口承载。

## 主要功能点

1. **沉浸式桌面交互（原生无感体验）**
   - **透明悬浮球置顶**：基于跨平台框架打造，以极小的系统资源占用悬浮于任何系统或 HIS 软件之上，双击唤起，随叫随到；启动恢复历史位置时按当前显示器 `workArea` 吸附到左右边缘并完整落在可见安全区域内，避免多屏切换、分辨率变化、Dock / 任务栏遮挡或虚拟桌面切换后不可见；小球状态下环绕悬浮按钮只跟随鼠标悬停显示，鼠标移出小球交互范围后必须隐藏，键盘或点击焦点不得单独保持按钮展开。
   - **单窗口灵活模式**：灵活模式场景不再额外打开悬浮小窗，而是直接进入智能问诊主界面的指定阶段；HIS `/assist` 与桌面工具栏入口共用同一条 `ConsultationPage` 灵活模式链路，已具备主诉、现病史时可直接跳过症状采集，减少上下文切换。

2. **统一就诊模式（完整流程 + 内嵌灵活模式）**
   - **完整流程保留**：现有症状采集、属性问诊、主诉/现病史生成能力继续保留，并且仍然是 HIS 接诊后的默认进入方式。
   - **灵活模式**：医生可在已接诊患者上下文中通过附加入口按需触发单个 AI 模块；系统根据 PHIS 已有字段自动判断是否跳过前置步骤，并将医生留在同一问诊页面中继续处理后续动作。

3. **检验检查报告解读（独立结果窗）**
   - **独立窗口承载**：检验检查报告解读不进入 `ConsultationPage`，而是通过独立窗口展示 AI 解读结果，避免打断正在编辑的问诊或语音结果页。
   - **上下文增强**：若当前已接诊患者，报告解读默认自动补入当前患者的性别、年龄、既往史/过敏史等可能影响结果解释的信息；若当前没有接诊患者，则允许 HIS / SDK 在入参中显式传入患者基础信息作为解读上下文。
   - **输入保持轻量**：HIS 侧只需传入 `taskId + query` 两个核心字段，`query` 可直接复用现有“报告日期/检查项目/检查结果/影像诊断”的拼接文本；桌面端负责与患者上下文合并并调用 AI。
   - **报告单式阅读**：报告解读窗口采用单页报告单版式，按“报告标题 / 患者与报告元数据 / 异常项目 / 综合判断 / 建议与注意事项”纵向组织，避免三栏卡片造成医生来回扫视。
   - **结果只读优先**：报告解读窗口优先强调“摘要结论、报告核心发现、异常/阳性结果、临床意义、建议动作、风险提示、适用范围与局限”，不进入 PHIS 回写闭环，也不修改当前问诊主页面状态。
   - **可读性与交互约束**：报告解读窗口内容超出可视区域时必须由窗口自身提供纵向滚动；窗口默认隐藏原生标题栏并统一使用页面内虚拟关闭按钮，窗口外壳、操作按钮、loading 与空态应沿用 floating-ball 的柔和玻璃态工作台风格；窗口顶部操作只保留打印/导出与关闭，不提供一键复制解读文本入口；打印/导出时必须按报告完整内容自然分页，不得受窗口高度或滚动容器裁切限制；桌面端应尽量从报告原文解析报告项目、报告时间、门诊号/样本号、申请/检验时间和异常指标，解析不到时以占位展示而不要求 HIS 额外传参；AI 输出若未给出足够具体的报告结论，桌面端需至少基于报告原文提炼出可直接供医生阅读的“核心发现 + 临床意义 + 下一步建议”。

3. **结构化与智能引导问诊（AI 驱动的症状网络）**
   - **智能病历引擎**：当前开放西医症状模板链路；中医模块仍处于开发中，医生点击中医入口时只提示“开发中”，不得进入中医模板、四诊采集或中医推荐链路。西医链路继续具备“伴随症状智能推荐”和“单选/多选/跳过”等复杂逻辑判定体系。
   - **自动病历生成**：医生仅需简单勾画点击，系统会调用大模型按基层全科个人模板风格生成“主诉”和“现病史”，优先区分急性症状、慢病复诊、检查异常和健康体检等门诊场景；模型不可用或输出不合格时，桌面端退回本地规则草稿，保证医生仍可继续进入病历详情并编辑。

4. **大模型结构化引擎数据映射**
   - **打通自由文本与业务系统的壁垒**：为了将大模型生成的自由文本型诊断/用药建议直接应用到医院现有的生产系统（HIS）中，内置了高性能的数据匹配机制。
   - **精准字典映射**：产品能毫秒级提取大模型给出的非结构化描述，并基于 HIS 同步后的标准目录与本地缓存完成词林映射；其中药品目录必须按当前有效发药药房 scope 使用，不再以 CSV 药品数据作为兜底，最终向生产系统输出标准化的结构数据草稿。

5. **AI 临床决策辅助 (CDSS)**
   - **大模型私有化接入**：支持无缝对接兼容 OpenAI 协议的各类大语言模型（如 DeepSeek 等）。
   - **智能推理**：面对不典型症状，AI 能够通过解析对话上下文提供高质量的诊断鉴别建议、开药指导和检查建议。
   - **建议可编辑**：所有 AI 推荐仅作为草稿，支持编辑、删除、替换，且修改后不中断当前问诊页面状态。
   - **配置归属明确**：在区域化模式下，模型、独立审查 AI、知识库等 API 配置由 `floating-ball-server` 后台统一维护；桌面端首启默认值中的后端地址优先取构建时注入的 `VITE_REGIONAL_BASE_URL`（CI/Release 由 GitHub Actions Repository Variables 注入），机构编码默认回退到 `ORG001`，如需覆盖只通过本地 `.env` 或设置页手工配置。设置页仍允许修改后端地址、机构编码或切回本地模式；开启区域化模式后隐藏“模型配置”页签，关闭后再恢复本地模型配置入口。保存接入参数时，即使后台暂时不可达，参数也会先保存成功，并单独提示当前连接状态。本地模式下仍保留桌面端手工配置兜底；“测试 server 连通性”仅验证桌面端到 `floating-ball-server` 的更新策略、注册与 bootstrap 链路，必须在请求超时或后台不可达时结束等待态并提示排查方向。
   - **设备身份基线**：区域化设备编码首次默认取当前机器 MAC 地址并持久化用于设备注册；只有在当前环境无法读取 MAC 时，才回退到本地兜底编码。Windows 端通过系统网卡 API 读取 MAC，不再拉起 `getmac` / `ipconfig` 等外部命令窗口。
   - **失败提示明确**：聊天入口、语音转写及其它直接依赖 AI 代理的交互，在后台未配置 AI 服务地址、密钥、模型或服务不可达时，必须立即结束等待态，并向医生展示明确错误原因；不允许出现持续转圈或仅保留空白回复气泡的状态。
   - **错误信息可理解**：医生可见错误必须说明“哪里失败、医生能做什么、必要时如何排障”。网络不可达、请求超时、后台 5xx、签名 / 鉴权失败、上游 AI 不可用、JSON 解析失败等技术异常不得原样展示为 `Load failed`、`Failed to fetch`、`Internal Server Error`、Java/SQL 类名或堆栈片段；区域化接口返回 `requestId` 时，提示中应带出请求 ID，便于后台管理员按日志定位。
   - **问题反馈闭环**：区域化模式下，医生可在任意工作视图的顶栏直接打开反馈入口，填写 1-5 分评分、文字说明，并使用内置截图或上传现成截图（如异常提示截图、回复结果截图）。反馈提交时会自动带上最近一次 AI 调用摘要，供后台排查。
   - **设置保存可见性**：系统设置中的“通用设置”和本地模式下的“模型配置”必须提供固定可见的保存入口，并明确提示未保存状态，避免医生在长表单中修改后因滚动遗漏保存。
   - **设置入口收敛**：系统设置当前只承载“通用设置”“关于版本”，并在本地模式下额外显示“模型配置”；不再提供“数据管理”页签或“症状库管理”入口，避免把统计、导出、缓存清理、模板维护等低频操作混入日常配置入口；基础数据缓存管理和 HIS 联调日志应像症状库管理一样从设置页独立入口打开，不混入“关于版本”内容。
   - **内网更新闭环**：关于版本页支持配置正式/测试内网更新源，区域化模式下默认指向 `floating-ball-server` 公开的 `/v1/client/releases/{channel}/latest.json`；管理员在后台上传 Tauri `latest.json` 与安装包后，桌面端通过 Tauri updater 完成检查、下载、签名校验、安装和重启。内网可使用 `http://` 更新源，但仍必须通过签名校验。后台发布版本时可勾选强制更新；强制更新生效后，低于最低可用版本的桌面端只能访问更新策略、检查更新和下载安装包，不得继续使用问诊、语音、知识库、AI 代理、模板同步、反馈等业务功能。

## 赋能基层医疗的核心场景

在基层医疗机构（如乡镇卫生院、社区卫生服务中心、村镇诊所），医生往往面临**全科化挑战大、时间紧凑、病历书写不规范、上级指导缺乏**等痛点。本产品主要在以下场景深度赋能：

### 特性 1：破解“看不了、看不准”的全科诊断难题
- **场景痛点**：基层全科医生知识面广但深度受限，特别是遇到相对生僻或跨科室症状时容易漏诊误诊。
- **赋能点**：当输入核心症状时，系统会自动进行**“伴随症状智能推荐”**，提示医生沿着推荐链路进行深度问诊排查；遇到疑难杂症，一键呼叫 AI 助手给出多维度的鉴别诊断。产品充当了一台全天候的“规培带教机器”和专家智库。

### 特性 2：拯救“耗时、易错”的病历书写泥潭
- **场景痛点**：每天面对庞大的就诊量，手敲病历耗费大量精力，且主诉和现病史的格式经常被质控部门打回。
- **赋能点**：通过“点选式”的高效问诊收集，智能文本引擎根据基层门诊模板风格和医学书写规范，生成可编辑的主诉、现病史草稿，并自动压缩无效问卷项、保留症状各自时间线和关键阴性。医生既可按完整链路一次生成，也可在医生站内按需触发生成并直接回写 HIS 草稿，将原本 3 分钟的书写时间压缩至 10 秒，腾出更多精力用于医患沟通。

### 特性 3：不打断门诊节奏的灵活辅助
- **场景痛点**：基层门诊接诊节奏快，医生常常只需要某一类建议，而非被迫走完整套 AI 流程。
  - **赋能点**：浮舱改为“同一问诊页面内灵活模式”的方式记录本次就诊辅助。医生既可从症状采集开始，也可直接带着 PHIS 草稿进入病历详情页，自动生成推荐诊断，并继续在同一页面完成诊断鉴别、推荐用药、推荐检查与病历回写。对于只需要一张处置清单的场景，HIS 可触发独立“诊疗方案推荐”页，基于当前主诉、现病史与诊断聚合用药、检查、检验、处置四类建议，医生勾选后仍按统一 `record-confirmed` 回写。对于院内已有检验/检查报告的二次解释需求，则通过独立报告解读窗快速给出辅助判断，不占用问诊主链路。
- **可理解性要求**：患者基础信息只作为原始上下文展示；AI 生成的主诉、现病史必须和 PHIS 原始字段明确区分；若 PHIS 已经有主诉、现病史，系统应直接用这些内容作为当前页面左侧病历详情的初始值。
- **语音问诊结果页可读性要求**：病例正文（主诉、现病史、既往史）优先级高于推荐依据说明；语音问诊结果页应优先保证医生一眼看到本次完整病例内容。右侧推荐区默认只展示“推荐结论 + 关键操作态”，长段“依据分析/主要依据/模型说明”默认折叠，医生按需主动展开查看。
- **患者基础栏过敏史展示要求**：智能问诊、语音问诊和独立诊疗方案等复用患者基础栏的页面，在接诊上下文存在明确过敏史时必须直接展示“过敏史：具体内容”，不得只展示泛化的“过敏史”标签；长内容可在基础栏内截断，但必须保留悬停查看完整过敏内容。
- **问诊结果页统一约束**：语音问诊和症状问诊进入结果编辑阶段后，必须共用同一套结果页组件与交互语义；两条链路只允许“问诊渠道 / 初始数据来源”不同，不允许长期并行维护两套结果页结构。共享诊断卡必须展示诊断置信度/匹配度（`Diagnosis.rate`）和推荐依据入口，语音问诊与智能问诊结果页都必须由共享结果页直接提供同款“诊断鉴别”入口和 checklist 弹窗，不再由 `ConsultationPage.vue` 维护独立调试弹框。诊断鉴别 checklist 必须同时覆盖两类情况：一是当前诊断与主诉、现病史明显不匹配或不能解释主要症状时，提示医生复核诊断方向，并按严重复核状态展示，默认展开完整错误内容，不得混入普通排查清单或空态；二是当前诊断虽基本成立但存在高危疾病混淆风险时，提示关键问诊或查体排除项。只有诊断与病历信息基本一致且无明显高危排查需求时，才允许返回空 checklist。症状问诊结果页必须保留底部“返回”入口，点击后只回到症状问诊编辑页继续补充，不触发放弃确认或清空当前结果；底部“放弃”则必须经过二次确认，确认后清空当前智能问诊快照和页面内勾选/推荐数据，并直接缩小回悬浮球，下次进入重新开始智能问诊。若症状问诊现有字段、编辑粒度、推荐项结构与语音结果页不一致，以语音结果页当前标准为准。
- **一键回写诊断安全约束**：症状问诊和语音问诊的一键回写都必须只把 PHIS 标准诊断库 ID 写入 `diagList.idDiag`，该值对应 PHIS `ID_DIE`；AI 自由文本、前端临时 key、PHIS 草稿诊断文本在未命中标准诊断库时只能用于展示和医生判断，不得作为可保存诊断提交。症状问诊切入共享结果页时必须保留已匹配的标准诊断 ID，未匹配诊断在提交前要明确拦截并提示医生先切换或重新匹配标准诊断。
- **一键回写医嘱非空安全约束**：用药、检查、检验、处置在勾选或最终一键回写前必须通过同一套必要字段校验。所有医嘱都必须有标准服务 ID、服务名称、服务分类编码、执行位置和医保限用；药品还必须有一次剂量、剂量单位、频次、用法、总量、天数和发药药房；检查必须有检查部位；检验必须有非空附加 `jsonField`；处置必须有大于 0 的数量。必填字段必须在编辑区用传统表单方式明确标识，当前值为空或被医生清空时输入框飘红并禁止勾选 / 一键回写，提示医生补齐或重新匹配标准库，不得把空字段写入 `orderList`。医生手动清空检查 / 检验 / 处置的执行科室，或清空任一医嘱的医保限用后，当前输入框空值就是最终状态，页面必须以红框提示并禁止选中或一键回写，不得再用标准库匹配项、详情 hydrate、默认医保类型或默认科室兜底通过校验。
- **标准项目详情反填约束**：AI 推荐检查 / 检验 / 处置时不得臆造执行科室；标准库匹配成功、候选确认或手动匹配后，必须通过 HIS 诊疗项目详情反填真实执行科室、单位和处置默认数量。执行科室只允许来自医生当前选择或详情接口返回的标准执行科室 ID；处置数量只在当前为空且医生未手动修改时，使用详情接口返回的默认数量。若详情缺失或未返回对应字段，页面继续按必填校验拦截医生补齐，不得以“治疗室”等固定值兜底。医生手动清空字段时，当前空值就是最终状态，后续 hydrate 不得自动补回。
- **共享结果页适配约束**：症状问诊和语音问诊进入结果编辑页前必须先转换成中性 `ClinicalResultInput`，再由结果页内部适配到当前 UI 所需结构。适配层必须完整保留上游已确认的 `matchedItem`、`suggestedMatchItem`、`matchStatus`、`manualMatched`、`selected`、发药药房、执行科室、检查部位、频次/用法 key、总量和医生编辑痕迹；只有上游缺少标准库匹配信息时，结果页才允许按当前目录重新评估匹配，不能覆盖医生已确认的数据。
- **智能问诊 AI 鲁棒性约束**：智能问诊的诊断、用药、检查、检验、处置等结构化 AI 调用必须向语音问诊结果页看齐：请求发起时不得先清空已有可用结果，只有在新响应成功解析并确认属于当前诊断上下文后才能覆盖；JSON 解析必须允许模型返回代码块或少量前后说明，并从响应中提取第一个完整 JSON 对象/数组；单路推荐失败时保留上一版结果并展示明确错误，不得让医生看到整页空结果；慢请求返回时若医生已切换到其他诊断，必须丢弃旧响应，避免覆盖新状态。共享结果页的“诊断建议”区必须提供医生手动触发的“刷新诊断”入口，允许基于当前病历重新进行 AI 诊断推荐；刷新期间保留上一版可用诊断，诊断区展示与治疗方案一致的内嵌 loading 占位，成功后再替换为新推荐，并按新主诊断提示医生手动刷新治疗方案。
- **症状问诊结果页病例补全约束**：症状问诊切入共享结果页时，既往史不得默认为空字符串。若 HIS / 接诊上下文已提供明确既往史，则直接使用；若缺少显式既往史但存在患者基础信息与历史就诊记录，应优先生成“既往门诊记录”式摘要作为初始既往史，供医生在结果页继续确认和编辑。
- **语音问诊结果页紧凑约束**：左侧病历正文区只保留必要宽度，右侧推荐区优先展示诊断/药品名称、规格和核心操作态；诊断区不再额外展示主诉/现病史概览面板，直接以标题明确“诊断建议”；治疗方案内的药品、检查、检验既然已经按分组展示，卡片标题行不得重复追加同类类型标签；名称、规格、匹配状态等辅助信息应尽量压缩在同一行展示，避免为辅助说明单独占行；依据说明改为紧贴诊断名、药品名、项目名后的图标提示，鼠标悬停和点击都应能看到完整内容；药品在未展开更多编辑时，也应直接在卡片头部展示“一次剂量、频次、用法、天数、总量”的文本摘要，减少医生额外点开成本；药品医嘱默认仅展开“一次剂量、频次、用法、总量”四类核心字段，其余字段收进头部箭头触发的“更多编辑”；这四类核心字段默认以可阅读的文本态呈现，医生点击后才进入编辑态，其中频次、用法必须保持可点击、可搜索、可修改，不得用置灰样式暗示禁用；规格、剂量单位、总量单位应优先展示模型返回值并保持只读，不再提供下拉编辑；药房、执行科室、医保限用等枚举字段在语音结果页应复用频次/用法同款的可搜索选择浮层，不再使用浏览器原生下拉框；这类可搜索选择器在已有值时再次打开，默认仍应展示完整候选列表，而不是被当前值锁定过滤；已有值的可搜索输入框右侧应提供小号清空按钮，候选列表只展示候选项；当发药药房存在多个有效候选时，药品项确认匹配或选中前必须按药房顺序轮询 `medicineDetail`，以第一个返回有效药品信息的药房作为该药品默认药房；若所有候选药房均无有效药品信息，必须提示医生且禁止选中该药品；药品匹配后，医生修改用药总量时必须调用 `api/phis.medicineInventoryService/checkInvEnough` 校验当前默认药房库存，库存不足提醒必须前置到“确认匹配 / 勾选 / 修改药房或总量”等单药操作当下，并明确带出具体药品名；提交前的库存校验仅作兜底，不应成为医生首次得知库存不足的时点；检查和检验的执行科室必须在卡片头部直观展示，并支持点击后直接打开可搜索选择器调整；执行科室为空时必须提示医生先设置，且禁止选中该检查/检验项目；执行科室已设置后，卡片头部只展示具体科室名称，不再重复显示“执行科室”标签；检查和检验的附加属性区中，执行科室应置于首位，医保限用后再展示规定病，备注放在最后；检查项目基础数据匹配成功后，必须通过 `api/phis.hiBdCliPacsPartService/queryExaPartAndWayList` 获取检查部位 / 方式候选；候选只有一项时默认选中，多项时在附加属性区提供可搜索选择器由医生调整；部位字段仅保留给检查项目，检验项目不再展示；检查与检验的总量字段不再给医生编辑，默认按 1 回写；发药药房、执行科室、检查部位与库存门禁必须覆盖语音问诊、智能问诊共享结果页、完整问诊遗留回写路径和独立诊疗方案页，任何入口不得绕过。当前已选中的诊断卡再次点击，或点击其推荐依据、反馈、同类诊断下拉、诊断鉴别等内部操作按钮时，不得被当作重新选择诊断，也不得触发治疗方案刷新；医生主动切到另一条诊断后，页面应保留现有方案并提示可手动点击“刷新方案”获取当前诊断方案；医生手动刷新后返回的结果也应尽量保持规格、一次剂量、频次、天数、总量等结构化字段，不得长期退化为仅有 usage 大文本；处置、检查、检验默认不展示编辑项，仅在医生主动展开时显示；当前页诊断项只允许单选切换，不允许点击后取消为空，也不再触发诊断鉴别排查弹窗。
- **语音问诊标准库匹配安全约束**：药品与诊疗项目（检查 / 检验 / 处置）必须采用保守匹配策略。只有名称完全匹配标准库时，才允许直接显示“匹配成功”并参与默认选中；仅达到高相似度的候选项应直接表现为“候选待确认”语义，并把“确认匹配”操作紧贴候选标准项展示，必须由医生显式点击确认后才可纳入回写；这类候选项提示区应以浅底色块和清晰边界突出显示，按钮样式需与提示区保持同一视觉语义，避免像独立悬浮操作；未命中标准库的项默认保持未匹配状态，不允许直接选中，医生只能通过“手动匹配”从标准库候选中明确选择后再纳入。
- **药品目录与发药药房约束**：药品标准库匹配不得再使用 CSV 药品数据兜底，只允许使用当前有效发药药房对应的 HIS 药品目录。发药药房下拉优先使用 `fetchAvailablePharmacies()` 返回的有效药房；若该接口无可用数据，才允许使用 `fetchMedicineStoreIds()` 返回的药房 `idSto` 作为兜底候选。自动匹配到药品后，发药药房候选必须收窄为该药品目录实际所在药房；未自动匹配时，手动标准库候选应来自全部当前可用药房对应的药品目录，医生手动匹配后再按所选药品的 `storeIds` 收窄药房。
- **语音问诊异步复核员约束**：语音生成病历和诊疗建议后，应异步启动安全复核，不阻塞医生阅读和编辑当前结果。复核员以“非干扰提醒”方式呈现，只提示可能影响首道安全底线的问题，例如危险信号遗漏、过敏/既往史冲突、当前建议与近期用药或病情信息的潜在风险；提醒文案必须可操作，优先给出“复查心电图 / 补问症状 / 考虑替代 / 关注禁忌”等临床动作，不直接替医生做处方决策。高危提醒在确认提交前需要医生知晓，但默认不强制拦截；中低危提醒不得打断主流程。当前阶段先基于语音结果、患者基础信息、既往史/过敏等本地上下文复核，后续接入 HIS 历史用药时必须通过独立安全上下文服务，不得把 HIS 查询逻辑塞回结果页。
- **语音问诊结果页视觉基线**：全页最小字号不得低于 13px；常规正文、标签、输入值默认使用 14px；重要信息如诊断名、药品名、核心字段值统一使用 14px 且加粗；选中态只允许使用贴左上角边框的轻量勾选标记，默认采用“淡底 + 深色图标”，不得再依赖整块背景色或高对比边框提示；语音问诊结果页的背景、卡片、按钮、标签和浮层应跟随系统配置中的风格设置自动换肤，不得长期写死为单一蓝白主题；整体氛围应以“上亮下稳”的轻层次背景为主，避免大面积自下而上的渐变或过重色块影响医生阅读；放弃和一键回写操作固定放在患者基础信息栏右侧，其中“放弃”需先经过二次确认，再清空当前患者未提交的语音缓存并直接退回小球状态；内容超出窗口高度时必须由结果页自身提供纵向滚动，不能裁切正文或推荐区；右侧推荐列的最外层容器必须由组件自身控制圆角和裁切，避免被全局面板样式打出尖角。
- **语音问诊反馈闭环约束**：语音结果页必须支持对单条诊断 / 治疗推荐做项级反馈，也必须支持对整页结果做 1-5 分整体评分与点评；项级反馈至少提供“有用 / 不满意 / 已修正采用”三类动作，其中“不满意”必须记录结构化问题标签与文字原因，“已修正采用”必须允许保留医生最终采用结果；病例详情中的主诉、现病史、既往史也必须提供字段级反馈入口，默认贴在字段标题旁，不得挤占正文阅读区域；若医生手动修改过字段，反馈数据中必须同时保留 AI 原文、医生当前值和可读的差异摘要，便于后续复盘具体改动；整页反馈不得常驻占用语音结果主界面，应在一键回写收到 HIS 成功回执后再以浮层方式弹出，并允许医生直接跳过关闭；该浮层不能影响回写成功本身，也不应把反馈填写前置为主流程门槛，但在低分场景下至少要求补充一个问题标签；一键回写成功只表示本次 HIS 回写闭环成功，不等同于语音问诊结束，同一就诊再次触发语音问诊入口时仍应恢复上一张语音结果页和编辑数据；所有反馈都应自动绑定当前会话、最近一次 AI 调用摘要、病例上下文摘要与推荐快照，先完成桌面端本地持久化，再由后续后台接口按标准 payload 推送。
- **交互约束**：主诉/现病史支持单独回写电子病历；诊断鉴别的确认只记日志、不修改病历；HIS 通过 `assist.action = diffDx` 单独触发鉴别诊断时，应直接打开独立“鉴别排查确认”小窗并开始生成建议，不进入后续问诊结果页 / 工作站页面；推荐诊断、推荐用药、推荐检查需要通过“引用”进入 PHIS 保存闭环，PHIS 保存成功或失败后再把结果回执给桌面端；症状问诊记录页底部的“一键回写”必须与语音问诊保持同一语义，直接触发最终 `complete_consultation` 结果写回，但结果页不能在请求发出后立刻结束，必须等待 HIS 通过 `reference-feedback` 回执最终成功/失败；失败时页面保持打开并展示错误原因，成功后才允许进入成功收口；PHIS 批量引用仍保留为独立能力，但症状问诊共享结果页当前阶段暂不展示单独“批量引用 PHIS”按钮，避免与最终提交形成双主操作；与第三方 HIS / PHIS 联调相关的本地 Bridge 入站请求和 PHIS 出站请求必须生成可检索、可导出、已脱敏的调用日志，并用 `traceId` 串联请求、响应和错误；推荐依据图标点击展开后，点击其它区域或触发失焦时应自动收起，不得要求医生再次点击同一图标关闭。
- **症状问诊推荐区视觉基线**：症状问诊记录页右侧的推荐方案应优先采用“医嘱清单式 worklist”视觉，而不是内容卡片流。推荐用药 / 检查 / 检验分组标题仅作为小节标题存在，默认不承担强装饰；组内每条推荐项应首先呈现“名称 / 规格 / 匹配状态 / 必填属性 / 核心操作”这一条稳定扫描路径，减少不同项之间的布局跳动；推荐依据、AI 原建议、候选标准项、处方摘要等补充信息允许压缩在主行下方，但不得破坏清单阅读节奏；执行科室 / 发药药房等必填属性应作为稳定属性列存在，避免与标题说明文字混排；医生确认勾选后才展开二级编辑区，避免默认把所有依据与编辑字段铺满一屏。
- **引用入口约束**：推荐诊断保持单选，并可在当前选中诊断上直接发起引用；推荐用药、推荐检查检验支持多选后按分组一次引入，不应退化成逐条重复点击引用。暂不支持 PHIS 引用的处置建议应明确标注为“仅展示、不可引用”。
- **独立诊疗方案推荐约束**：`assist.action = treatment_plan` 承载独立聚合方案页，只复用当前接诊上下文中的主诉、现病史、既往史、过敏史和诊断，不启动完整症状问诊。页面按推荐药品、推荐检查、推荐检验、推荐处置四组展示，默认以清单勾选为主，不暴露完整病历编辑器；但每条推荐项必须复用语音问诊 / 智能问诊共享结果页的二次编辑能力，允许医生继续调整药品的一次剂量、频次、用法、总量、天数、发药药房、医保限用、规定病和备注，以及检查 / 检验 / 处置的执行科室、检查部位、医保限用、规定病和备注。检查 / 检验数量固定按 1 回写，不在页面展示数量编辑入口，也不允许医生修改；处置项目必须优先按 HIS 项目详情反填的默认数量或医生确认后的总量回写，缺失时拦截医生补齐，不以 1 兜底。药品、检查、检验、处置的标准库匹配、候选确认、手动匹配、药品详情 hydrate、执行科室 / 检查部位 / 发药药房门禁和库存校验必须复用语音问诊同一套共享能力；一键回写必须复用 `record-confirmed` 的 `diagList/orderList` 构造器，等待 PHIS 通过 `/reference-feedback` 回执后再展示最终成功或失败。
- **住院病历历史门诊引用约束**：入院记录补充要点弹窗中的历史门诊卡片只展示科室、日期和主诊断，不重复展示主诉摘要；主诉、现病史等正文事实以“查看”打开的门诊病历 HTML 内容为准，避免列表卡片出现“无主诉记录”等占位文案误导医生。医生引用带正文的门诊病历后，门诊病历与手工/语音补充要点应共同作为入院记录书写依据；仅在既无补充要点、也无可用门诊病历正文时，才停留在等待输入状态。AI 生成必须以入院记录书写角度综合门诊病历、补充信息和住院上下文重新归纳主诉、现病史、入院情况等字段，不得把门诊主诉、现病史或门诊正文原样搬入入院记录。病历预览中的页眉标题等非 AI 模板字段必须保留 HIS 模板原始默认值，不得统一替换为“病程记录”等固定文案。
- **闭环约束**：点击引用后，桌面端应保持当前问诊窗口展开，允许医生继续查看和补充本次问诊内容；PHIS 保存完成后再通过本地回执接口返回成功/失败原因，并同步更新当前页面状态。当前实现仍属于同一运行期内的内存保活，不代表重启应用后仍能自动恢复。
- **悬浮球门禁与最小化语义**：未接诊患者或当前患者不存在未结束问诊时，悬浮球的问诊按钮必须保持可见但禁用，不得借此主动新开症状问诊或语音问诊；语音问诊只能由 HIS / SDK 推送的 `start-voice-consultation` 触发，且患者上下文缺失时立即拒绝。症状问诊和语音问诊点击顶栏收起或关闭时，必须先统一回到接待胶囊（风险提示页）；接待胶囊的收起落点尺寸以风险胶囊基线为唯一标准，HIS 直接进入胶囊和从问诊界面收起到胶囊不得出现两套尺寸。医生从接待胶囊再次退出到悬浮球时，才按"最小化"处理：原问诊界面的现场状态保留在内存中，对应的悬浮球恢复入口（症状问诊为环菜单按钮，语音问诊为双击小球）继续保持"恢复"语义，可直接还原回原界面而不重新初始化；同一医生可同时存在症状和语音两路最小化会话，互不影响，且从接待胶囊退出时不得覆写原有最小化类型；大面板收起到胶囊时，只允许切换外层视图和窗口尺寸，不得复位 `ConsultationPage` 内部页签或清空症状问诊数据，避免医生回到问诊时从结果页/编辑页被重置到症状选择页。症状问诊的页面留存必须与语音问诊一致：未诊毕、未确认放弃时，再次点击“智能问诊”或最小化后再次打开，都应恢复到上次停留的内部页面（症状采集、病历详情或最终报告）和当时的数据；智能问诊确认放弃后必须立即清除当前数据并退回悬浮球，不得回到症状界面继续保留旧勾选；语音问诊在一键回写成功后仍属于未诊毕现场，同一就诊再次点击语音问诊必须恢复上一张数据界面，不得重新进入录音问诊；但一旦当前接诊上下文从患者 A 切换到患者 B，语音问诊也必须和智能问诊一致，把患者 A 的未结束语音现场视为已失效并清理，之后再切回患者 A 也应重新开始语音问诊；只有诊毕、确认放弃、患者切换或跨自然日失效时才清除快照。最小化记录以就诊 ID（`idVis`）为锚点，跨自然日自动失效；恢复时使用医生上次手动调整过的窗口尺寸（沿用 `useWindowManagement` 已有的视图级持久化）。
- **语音问诊整张病历的缓存恢复**：同一就诊从缓存恢复语音问诊时，必须复用上次落到编辑器里的整张病历快照（主诉、现病史、既往史、诊断、诊疗方案、当前选中诊断、上一次 fetchAITreatment 对应的诊断键）。除非缓存丢失或跨自然日已失效，否则不得在恢复时再次触发 `fetchAITreatment`，避免医生看到诊疗方案"被重新刷一遍"。`useVoiceConsultation` 的 `editorSnapshot` 是该快照的唯一落点；编辑器内任何字段变化都按节流写回，fetchAITreatment 完成后立即写回。
