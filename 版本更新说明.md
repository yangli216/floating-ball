# 版本更新说明

## 1. 文档目标

本文档说明 `floating-ball / MedHermes` 当前已经落地的版本更新机制，重点覆盖以下内容：

1. 如何通过 GitHub Actions 构建 Windows 内网更新包
2. 如何发布一个新版本
3. 如何搭建和维护内网更新服务器
4. 如何把新版本发布到内网更新服务器
5. 客户端如何配置测试/正式更新源
6. 如何验证更新链路是否正常

本文档以当前仓库实现为准，关键实现文件如下：

- [`.github/workflows/release.yml`](/Users/yangl/IdeaProjects/floating-ball/.github/workflows/release.yml)
- [`scripts/release.mjs`](/Users/yangl/IdeaProjects/floating-ball/scripts/release.mjs)
- [`scripts/package-windows-internal-update.mjs`](/Users/yangl/IdeaProjects/floating-ball/scripts/package-windows-internal-update.mjs)
- [`src/components/UpdateChecker.vue`](/Users/yangl/IdeaProjects/floating-ball/src/components/UpdateChecker.vue)
- [`src/services/updateConfig.ts`](/Users/yangl/IdeaProjects/floating-ball/src/services/updateConfig.ts)
- [`src-tauri/src/lib.rs`](/Users/yangl/IdeaProjects/floating-ball/src-tauri/src/lib.rs)
- [`src-tauri/tauri.conf.json`](/Users/yangl/IdeaProjects/floating-ball/src-tauri/tauri.conf.json)

## 2. 当前更新机制概览

当前版本更新机制由三部分组成：

1. GitHub Actions 负责构建安装包、生成签名文件、整理 Windows 内网更新包目录，并上传为 workflow artifact
2. 内网 HTTP 静态服务器负责托管 `latest.json`、安装包和签名文件
3. 客户端设置页负责配置测试/正式内网更新地址，Rust 运行时根据配置动态切换 updater endpoint

当前机制有两个重要特性：

1. `tauri.conf.json` 中仍保留默认 updater endpoint，作为兜底地址
2. 客户端一旦配置了内网 `latest.json` 地址，会优先使用内网地址，不再依赖默认源

## 3. GitHub Actions 现状

当前发版工作流见 [`release.yml`](/Users/yangl/IdeaProjects/floating-ball/.github/workflows/release.yml)。

### 3.1 触发方式

支持两种触发方式：

1. 推送 tag 自动触发
   - 匹配规则：`v*`
   - 例如：`v1.2.10`
2. 手动触发 `workflow_dispatch`

### 3.2 手动触发参数

`workflow_dispatch` 支持以下输入参数：

1. `tag`
   - 必填
   - 例如 `v1.2.10`
2. `channel`
   - 非必填
   - 默认值：`stable`
   - 用于区分测试通道或正式通道
3. `publish_base_url`
   - 非必填
   - 用于指定内网更新根地址
   - 例如 `https://intra.example.com/med-hermes/stable`
4. `release_notes`
   - 非必填
   - 写入 `latest.json` 的更新说明

### 3.3 工作流实际做了什么

当前 Release 工作流会执行以下动作：

1. 检出代码
2. 安装 Rust / Node / Yarn 依赖
3. 构建前端
4. 调用 `tauri-apps/tauri-action` 构建安装包
5. 生成 Tauri updater 所需签名产物
6. 在 Windows runner 上执行 [`package-windows-internal-update.mjs`](/Users/yangl/IdeaProjects/floating-ball/scripts/package-windows-internal-update.mjs)
7. 生成内网更新目录结构
8. 上传 `dist/intranet-update/**` 作为 GitHub Actions artifact

需要明确一点：

当前工作流会“构建并整理内网发布包”，但不会自动把产物上传到你们的内网服务器。当前最后一步是上传为 GitHub artifact，后续仍需要人工下载并发布到内网静态服务器，或者后续再扩展一段自动部署逻辑。

## 4. 发布前准备

在 GitHub Actions 可正常工作前，需要先配置仓库变量和 secrets。

### 4.1 必备 Secrets

以下 secrets 直接参与 Tauri 构建与签名：

1. `TAURI_SIGNING_PRIVATE_KEY`
   - Tauri updater 私钥
   - 用于生成 updater 校验签名
2. `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`
   - 私钥密码

### 4.2 必备 Variables

当前 workflow 使用了以下 repo variables：

1. `INTRANET_UPDATE_BASE_URL`
   - 当手动触发没有显式填写 `publish_base_url` 时使用
   - 例如 `https://intra.example.com/med-hermes/stable`
2. `VITE_REGIONAL_BASE_URL`
   - 桌面端连接 `floating-ball-server` 的默认地址
3. `VITE_REGIONAL_ORG_CODE`
   - 可选；默认机构编码，未配置时客户端回退 `ORG001`

模型、语音和知识库供应商凭据由 `floating-ball-server` 托管，不再作为桌面端 GitHub Secrets 或构建变量注入。

### 4.3 Tauri 配置前提

[`tauri.conf.json`](/Users/yangl/IdeaProjects/floating-ball/src-tauri/tauri.conf.json) 中已经具备以下关键配置：

1. `bundle.createUpdaterArtifacts = true`
   - 构建时生成 updater 所需产物
2. `plugins.updater.active = true`
3. `plugins.updater.pubkey`
   - 客户端用公钥校验签名
4. `plugins.updater.endpoints`
   - 默认兜底源
5. Windows 专用 [`tauri.windows.conf.json`](/Users/yangl/IdeaProjects/floating-ball/src-tauri/tauri.windows.conf.json)
   - `bundle.targets = "msi"`，Windows release 只生成 MSI，不再同时生成 NSIS `.exe`
   - `bundle.windows.wix.language = "zh-CN"`，MSI 文件名和安装界面使用中文语言包

注意：

`pubkey` 必须和 `TAURI_SIGNING_PRIVATE_KEY` 对应，否则客户端会出现签名校验失败。

## 5. 如何发布新版本

建议把“版本号提升”和“GitHub 构建”分成两个动作。

### 5.1 第一步：本地提升版本号并打 tag

当前仓库已经有版本脚本 [`scripts/release.mjs`](/Users/yangl/IdeaProjects/floating-ball/scripts/release.mjs)。

可执行：

```bash
yarn release patch
```

或：

```bash
yarn release minor
```

或：

```bash
yarn release major
```

这个脚本会自动完成：

1. 更新 [`package.json`](/Users/yangl/IdeaProjects/floating-ball/package.json) 的版本号
2. 更新 [`src-tauri/tauri.conf.json`](/Users/yangl/IdeaProjects/floating-ball/src-tauri/tauri.conf.json) 的版本号
3. 更新 [`src-tauri/Cargo.toml`](/Users/yangl/IdeaProjects/floating-ball/src-tauri/Cargo.toml) 的版本号
4. 执行 `git add`
5. 提交 `chore: release vX.Y.Z`
6. 创建 `vX.Y.Z` tag

然后执行：

```bash
git push
git push origin vX.Y.Z
```

推送 tag 后，会自动触发 Release workflow。

### 5.2 第二步：也可以直接手动触发 Release workflow

如果版本号和 tag 已经存在，也可以直接去 GitHub Actions 页面手动运行 `Release` 工作流，填写：

1. `tag`
2. `channel`
3. `publish_base_url`
4. `release_notes`

推荐场景：

1. 需要重新打同一个 tag 的构建产物
2. 需要给测试通道和正式通道分别生成一套内网目录
3. 需要临时覆盖 `publish_base_url`

### 5.3 第三步：下载 GitHub Actions Artifact

Release workflow 执行完成后，在 Windows job 中会上传一个 artifact：

```text
windows-internal-update-<tag>
```

例如：

```text
windows-internal-update-v1.2.10
```

下载后可得到类似目录：

```text
dist/intranet-update/
  stable/
    latest.json
    release-manifest.json
    v1.2.10/
      xxx_zh-CN.msi
      xxx_zh-CN.msi.sig
```

这里就是要发布到内网服务器的内容。

## 6. 内网更新包结构说明

Windows 内网包整理逻辑在 [`package-windows-internal-update.mjs`](/Users/yangl/IdeaProjects/floating-ball/scripts/package-windows-internal-update.mjs)。

脚本会从 Tauri Windows 构建目录中查找：

1. 安装包 `.msi`
2. 对应签名文件 `.msi.sig`

然后输出为以下结构：

```text
<output-dir>/
  <channel>/
    latest.json
    release-manifest.json
    v<version>/
      <installer>.msi
      <installer>.msi.sig
```

例如：

```text
intranet-update/
  stable/
    latest.json
    release-manifest.json
    v1.2.10/
      MedHermes_1.2.10_x64_zh-CN.msi
      MedHermes_1.2.10_x64_zh-CN.msi.sig
```

### 6.1 `latest.json` 的作用

客户端检查更新时访问的是 `latest.json`，其内容由脚本自动生成，包含：

1. `version`
2. `notes`
3. `pub_date`
4. `platforms.windows-x86_64.url`
5. `platforms.windows-x86_64.signature`

其中：

1. `url` 指向具体安装包下载地址
2. `signature` 不是文件路径，而是 `.sig` 文件里的文本内容

### 6.2 `release-manifest.json` 的作用

`release-manifest.json` 不是客户端 updater 必需文件，主要用于：

1. 发布后核对产物是否齐全
2. 给运维或实施同事做目录校验
3. 辅助排查“上传漏文件”问题

## 7. 如何搭建内网更新服务器

当前客户端只要求能通过 HTTP/HTTPS 访问静态文件，因此内网服务器不需要特殊后端逻辑，静态文件服务即可。

可选方案包括：

1. Nginx
2. Apache
3. IIS
4. 对象存储网关
5. 任意能稳定提供静态文件下载的内部文件服务

### 7.1 推荐目录结构

建议按产品名和通道组织目录：

```text
/med-hermes/
  stable/
    latest.json
    release-manifest.json
    v1.2.9/
    v1.2.10/
  testing/
    latest.json
    release-manifest.json
    v1.2.10/
```

推荐对应 URL：

```text
https://intra.example.com/med-hermes/stable/latest.json
https://intra.example.com/med-hermes/testing/latest.json
```

### 7.2 Nginx 示例

如果使用 Nginx，可参考：

```nginx
server {
  listen 80;
  server_name intra.example.com;

  location /med-hermes/ {
    alias /data/intranet-update/;
    autoindex on;
    add_header Cache-Control "no-cache";
  }
}
```

如果你的静态目录是：

```text
/data/intranet-update/stable/latest.json
/data/intranet-update/testing/latest.json
```

那么访问地址可对应为：

```text
http://intra.example.com/med-hermes/stable/latest.json
http://intra.example.com/med-hermes/testing/latest.json
```

### 7.3 服务器要求

至少要满足以下要求：

1. 客户端机器能访问该地址
2. `latest.json` 可直接打开
3. `.msi` 文件可直接下载
4. `.msi.sig` 文件保留原始文本内容，不要被网关改写
5. 不要对 `latest.json` 做长期缓存

## 8. 如何把新版本发布到内网服务器

当前建议采用“GitHub Actions 构建，人工下载 artifact，再上传到内网服务器”的方式。

标准操作如下：

1. 触发 Release workflow
2. 等待 Windows job 构建完成
3. 下载 `windows-internal-update-<tag>` artifact
4. 解压后得到 `dist/intranet-update/`
5. 将其中对应 channel 目录上传到内网静态服务器
6. 覆盖服务器上对应的 `latest.json`
7. 保留历史 `vX.Y.Z/` 目录，便于回滚

### 8.1 发布测试版本

假设本次发布版本为 `v1.2.10`，测试地址为：

```text
https://intra.example.com/med-hermes/testing/latest.json
```

则 workflow 触发时建议：

1. `channel = testing`
2. `publish_base_url = https://intra.example.com/med-hermes/testing`

上传后服务器应得到：

```text
/med-hermes/testing/latest.json
/med-hermes/testing/release-manifest.json
/med-hermes/testing/v1.2.10/xxx_zh-CN.msi
/med-hermes/testing/v1.2.10/xxx_zh-CN.msi.sig
```

### 8.2 发布正式版本

正式版本建议：

1. `channel = stable`
2. `publish_base_url = https://intra.example.com/med-hermes/stable`

上传后服务器应得到：

```text
/med-hermes/stable/latest.json
/med-hermes/stable/release-manifest.json
/med-hermes/stable/v1.2.10/xxx_zh-CN.msi
/med-hermes/stable/v1.2.10/xxx_zh-CN.msi.sig
```

### 8.3 回滚建议

如果新版本有问题，不一定要重新构建旧版本，可以直接把对应通道的 `latest.json` 改回上一稳定版本。

例如把 `stable/latest.json` 中的内容切回 `v1.2.9` 对应的安装包 URL 和签名。

前提是旧版本目录还保留在服务器上。

## 9. 客户端更新配置说明

客户端界面入口在 [`UpdateChecker.vue`](/Users/yangl/IdeaProjects/floating-ball/src/components/UpdateChecker.vue)。

### 9.1 可配置项

客户端支持以下配置：

1. 当前环境
   - `production`
   - `testing`
2. 正式内网地址
3. 测试内网地址

对应持久化逻辑见 [`updateConfig.ts`](/Users/yangl/IdeaProjects/floating-ball/src/services/updateConfig.ts)。

### 9.2 本地存储字段

客户端通过 `localStorage` 保存以下字段：

```text
UPDATE_ENVIRONMENT
INTRANET_UPDATE_PRODUCTION_URL
INTRANET_UPDATE_TESTING_URL
```

### 9.3 环境变量默认值

如果本地未配置，前端会读取以下默认值：

```text
VITE_UPDATE_ENVIRONMENT
VITE_INTRANET_UPDATE_PRODUCTION_URL
VITE_INTRANET_UPDATE_TESTING_URL
```

### 9.4 客户端的实际行为

运行逻辑如下：

1. 前端根据当前环境取出对应 URL
2. 调用 Tauri 命令 `check_app_update`
3. Rust 侧通过 `build_runtime_updater()` 动态构建 updater
4. 如果前端传入 endpoint，则覆盖默认 updater 地址
5. 如果没有传入 endpoint，则回退到 [`tauri.conf.json`](/Users/yangl/IdeaProjects/floating-ball/src-tauri/tauri.conf.json) 中默认 endpoint

也就是说：

1. 配了测试/正式内网地址时，客户端优先走内网
2. 没配地址时，客户端走默认源

### 9.5 推荐配置方式

正式环境建议填写：

```text
https://intra.example.com/med-hermes/stable/latest.json
```

测试环境建议填写：

```text
https://intra.example.com/med-hermes/testing/latest.json
```

不要只填目录地址，必须填完整的 `latest.json`。

## 10. 客户端更新操作说明

客户端标准操作路径如下：

1. 打开系统设置
2. 进入版本更新模块
3. 选择当前环境
4. 填写更新地址
5. 点击“保存更新源”
6. 点击“检查更新”
7. 如果发现新版本，点击“立即更新并重启”
8. 等待下载完成
9. 应用自动重启

下载过程中的进度事件为：

```text
update-download-progress
```

安装完成后，前端会调用 `relaunch()` 重启应用。

## 11. 版本发布后的验证清单

建议分成“服务器验证”和“客户端验证”两部分。

### 11.1 服务器验证

发布完成后，先人工验证以下内容：

1. `latest.json` 是否可以通过浏览器直接访问
2. `latest.json` 里的 `version` 是否正确
3. `latest.json` 里的 `url` 是否指向正确的 `.msi`
4. `latest.json` 里的 `signature` 是否非空
5. `vX.Y.Z/xxx_zh-CN.msi` 是否可下载
6. `vX.Y.Z/xxx_zh-CN.msi.sig` 是否存在

### 11.2 客户端验证

用一台旧版本客户端验证：

1. 打开设置页的“版本更新”
2. 选择测试或正式环境
3. 填写并保存内网 `latest.json` 地址
4. 点击“检查更新”
5. 应出现新的版本号和更新说明
6. 点击“立即更新并重启”
7. 等待下载完成并自动重启
8. 重启后确认版本号已更新

### 11.3 推荐测试顺序

推荐先测 `testing`，后发 `stable`：

1. 发布测试通道
2. 测试客户端更新
3. 核对升级后基本功能
4. 确认无误后再发布正式通道

## 12. 常见问题排查

### 12.1 GitHub Actions 构建失败

优先检查：

1. `TAURI_SIGNING_PRIVATE_KEY` 是否正确
2. `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` 是否正确
3. 构建依赖的 `VITE_*` 变量是否配置完整
4. tag 是否符合 `vX.Y.Z` 规范

### 12.2 Workflow 报 `INTRANET_UPDATE_BASE_URL is required`

这是因为工作流在 Windows 打包步骤要求必须拿到内网发布根地址。

解决方式二选一：

1. 手动触发 workflow 时填写 `publish_base_url`
2. 在 GitHub 仓库变量中配置 `INTRANET_UPDATE_BASE_URL`

### 12.3 客户端提示“当前已是最新版本”

优先检查：

1. `latest.json` 中的版本号是否高于当前客户端版本
2. 客户端当前环境是否选择正确
3. 更新地址是否写到了 `latest.json`
4. 服务器缓存是否导致旧的 `latest.json` 仍被返回

### 12.4 下载成功但安装失败

优先检查：

1. `.sig` 是否与当前 `.exe` 匹配
2. `latest.json` 中的 `signature` 是否来自正确的 `.sig` 文件
3. `pubkey` 是否与签名私钥匹配
4. 杀毒软件或系统策略是否拦截安装包

### 12.5 客户端没有走内网更新地址

优先检查：

1. 当前环境是否选错
2. 该环境的 URL 是否为空
3. 地址是否是完整 `latest.json`
4. 地址是否为合法 URL

## 13. 推荐发布流程

建议按以下顺序执行：

1. 本地整理代码并确认可发布
2. 执行 `yarn release patch|minor|major`
3. 推送 commit 和 tag
4. 等待 GitHub Actions 构建完成
5. 下载 `windows-internal-update-<tag>` artifact
6. 先上传到 `testing` 通道
7. 用测试客户端验证完整升级链路
8. 验证通过后再上传到 `stable` 通道
9. 记录本次发布说明和回滚点

## 14. 后续可继续优化的点

当前方案已经能稳定支撑“GitHub 构建 + 内网分发”，但还有几个可继续增强的方向：

1. 在 GitHub Actions 中增加自动上传到内网服务器的步骤
2. 为 `testing` 和 `stable` 分别增加独立 workflow
3. 增加内网 `latest.json` 健康检查
4. 在客户端设置页显示当前实际生效的 endpoint 和最近一次检查结果

## 15. 一句话总结

当前仓库已经具备完整的“GitHub Actions 构建 Windows 内网更新包 + 内网静态服务器分发 + 客户端动态切换测试/正式更新源”的机制。现在的缺口不在构建，而在“产物下载后上传到内网服务器”这一步仍是人工操作。
