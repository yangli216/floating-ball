# floating-ball-server 接入配置示例
# 本地开发时复制此文件为 .env；第三方 AI/语音/知识库密钥只配置在服务端

# 启用测试模式
VITE_SPEECH_TEST_MODE=false

# 唯一运行形态由 floating-ball-server 托管，后端地址优先由 GitHub Actions Repository Variables 注入
# 机构编码默认回退到 ORG001；如需本地覆盖，可在 .env 中显式声明
# 未配置时回退到 http://127.0.0.1:8080 + ORG001
# VITE_REGIONAL_BASE_URL=http://127.0.0.1:8080
# VITE_REGIONAL_ORG_CODE=ORG001
