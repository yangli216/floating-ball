# 设计决策摇摆记录 (DECISION_DRIFT.md)

> **用途**: 记录历史上反复变更、撤回、合并、拆分或默认值来回调整的设计决策。
> 这类记录不是普通 bug 复盘，而是提醒后续 agent：这些问题的团队立场尚未完全稳定，改动前必须先确认当前取向。
>
> **维护规则**:
> 1. 每条记录必须包含：决策点、摇摆历史、当前状态、改动前问。
> 2. 发现同一设计点在一次或多次任务中被反复推翻、重做、拆分又合并、默认值来回切换时，必须追加记录。
> 3. 如果当前状态已经收敛为架构、产品或协作硬约束，必须同步更新 `ARCHITECTURE.md`、`PRODUCT.md` 或 `AGENTS.md`，并在本文件标注已收敛位置。
> 4. 记录的是"为什么这里容易摇摆"，不要只写最后一次实现结果。

---

## 当前摇摆点

| 决策点 | 摇摆历史 | 当前状态 | 改动前问 |
| --- | --- | --- | --- |
| **业务AI vs 审核AI 是否分离** | 最初统一 -> 拆分独立配置 -> 增加开关 -> 默认关闭 | 可选独立审核AI，默认关闭 | "审核AI走独立模型还是复用业务模型？" |
| **思考模式(reasoning)开关** | 默认开 -> 发现响应太慢 -> 默认关 | 默认关闭，用户可手动开 | "当前场景是否需要思考模式？" |
| **中西医数据模型** | 最初只有西医 -> 加中医诊断 -> ID冲突修复 -> 中药不匹配药品库 | 中西医各自独立数据源和模板 | "改动涉及中医还是西医？数据源是否隔离？" |
| **事实核查的植入方式** | 独立组件 -> 嵌入问诊页 -> 嵌入语音结果 -> 嵌入聊天 | 多处嵌入，缺少统一组合机制 | "事实核查是页面级还是全局级？" |
| **症状选择入口** | 单一列表 -> +部位选择 -> +系统分类 -> +搜索 -> +自定义录入 | 三种模式并行 + 自定义兜底 | "新的选择方式是替代还是新增？" |
| **复诊配药候选、确认与等待承载** | 初版要求近一年最多 5 次中诊断与药品都重复至少 2 次 -> 最近 3 次慢病就诊 -> 近 90 天时间窗 -> 多慢病增加独立确认页并串行生成确认计划、病历 -> 医保周期核查拆出完整同药流水 -> 为降低等待取消独立确认生成，改为病历与核查项一次返回、结果页确认 -> 单次生成仍在胶囊阻断 10 秒左右，改为先进入结果页显示真实阶段 -> 实测内容仍需等完整 JSON 一次性出现，最终改为确定性预填 + 单次 SSE 分段更新，并把复诊核查移到主诊断锚定浮层 | 已收敛为同一近 90 天查询上的双派生、单次流式模型生成与渐进结果页：自动沿用和病历生成受医生确认病种范围约束，药品周期核查独立读取全部同药处方；单一慢病直接生成，多慢病只做本地范围选择；确定性历史事实先展示，病历/核查/药品逐段到达，迟到响应经会话 token 拦截；复诊核查替代慢病场景普通主诊核查，并仍需医生显式确认后才写入病历，见 `PRODUCT.md` 3B 与 `ARCHITECTURE.md` 患者上下文基线 | "本次读取历史是为了自动沿用还是周期核查？是否重新引入独立确认页、阻断等待、完整 JSON 一次性落地、两次模型调用、双核查浮层，或把推荐值提前写成患者事实？" |
| **报告解读承载形态** | 初版只允许 HIS / SDK 单份报告进入独立窗口，以避免打断问诊；新增接诊后近期报告入口时，若继续为每份报告开独立窗口会割裂历史浏览与报告回诊 | 已收敛为“双入口共用能力”：外部单报告仍用独立窗口，风险胶囊进入主窗口报告工作台；工作台内完成历史浏览和单份解读，并可从与本次回诊关联的报告升级到诊疗方案，见 `PRODUCT.md` 3/3A 与 `ARCHITECTURE.md` 14、11.1/11.2 | "当前改动是在维护外部单报告窗口，还是在扩展应用内报告工作台？是否仍复用同一解读内核和正文组件？" |
| **报告工作台 AI 触发时机** | 初版选择报告即自动生成 AI 解读，原始报告只在生成等待期短暂展示；医生实际更需要先核对原始结果，再决定是否消耗 AI 解读 | 已收敛为“原始报告默认 + 医生手动触发”：选中报告只加载并展示结构化原始结果，点击“开始 AI 解读”后才调用模型；结果按报告做会话缓存，并可在原始报告 / AI 解读间切换，见 `PRODUCT.md` 3 与 `ARCHITECTURE.md` 1.3 | "选择报告是否会偷偷触发 AI？解读后是否仍能返回同一份原始报告？" |
| **药品一次剂量是临床剂量还是制剂数** | 提示词曾要求模型返回 `targetDose`，前端再换算为“几片几粒”；PHIS 实际回写字段却是 `doseOnce + unitDose` 临床剂量，旧 hydrate 曾把制剂比值 `1` 与质量单位 `g/mg` 拼接，报告回诊因而出现 `1g/1mg`；后续又发现模型总量、hydrate 后总量和库存校验时总量可能来自不同阶段 | 已收敛为：`targetDose` 先换算到 PHIS `unitDose`；片/粒/袋数只由程序参与包装总量计算，药品详情加载后必须重新归一并用最终总量校验库存，模型包装总量不进入处方事实，见 `PRODUCT.md` 药品推荐定稿安全约束与 `ARCHITECTURE.md` 第 10 条 | "本次药品是否完整经过库存对齐、详情 hydrate、剂量换算、标准频次/用法、程序总量和最终库存校验？" |
| **复诊配药现病史由规则模板还是动态确认生成** | 初版直接由模型/兜底模板生成“病情控制、依从性待核实”式现病史；医生案例更偏自然临床叙事，但自动补“规律服药、病情平稳、无不适”又会制造未确认事实；后续引入动态确认后，文字/语音补充曾被设计为再次生成确认项，实测增加等待且改变了医生已浏览的选项 | 已收敛为：病历草稿和最少核查项由模型一次生成，默认推荐不等于确认；医生在结果页点击选项后，客户端只增量写入该选项的 `recordText`，不再调用第二次病历模型，也不覆盖医生手工文本，见 `PRODUCT.md` 3B、`ARCHITECTURE.md` 患者上下文基线 | "本次改动会不会让推荐值未确认即进入病历，或再次引入第二次生成等待？" |
| **客户端保留本地/区域双模式还是固定服务端托管** | 开发初期同时维护客户端直连 LLM/语音/PMPHAI、本地 Tauri 代理与区域服务代理；随后区域模式成为默认，但开关、模型设置页、客户端密钥、Rust 代理和本地统计仍长期并存，形成两套出口与契约漂移风险 | 已收敛为服务端托管单一模式：AI、语音、知识库、Prompt/模板、反馈和审计只走签名 `/v1/*`；客户端不保存第三方凭据，上游地址也不再通过 bootstrap 下发。HIS Bridge、HIS Adapter、目录缓存和桌面原生能力继续保留，见 `PRODUCT.md`、`ARCHITECTURE.md`、`AGENTS.md` 当前架构基线 | "这是服务端托管业务能力，还是必要的本机 HIS/桌面基础设施？是否会重新引入客户端第三方出口？" |
| **HIS 结果通道只用 WebSocket 还是保留长轮询兜底** | 初版轮询 -> 增加 WebSocket 并保留 poll -> 2026-06-12 同时删除 SDK 轮询与 Rust poll 路由 -> 2026-06-22 为解决旧 HIS 回写漏收只恢复 SDK 轮询和文档、未恢复服务端路由，形成半套 fallback，并在桌面端退出后持续产生 `handshake + poll` 失败请求 | 已收敛为 `/api/consultation/events/ws` 唯一结果通道；SDK 仅做带 `event.id` 的 WebSocket 重连，离线握手采用指数退避，禁止恢复 `/api/consultation/events/poll`，见 `ARCHITECTURE.md` 交互目标、`api.md` 6.4 与 `AGENTS.md` Review 门禁 | "这是在增强 WebSocket 重连，还是准备重新引入第二套结果通道？若要恢复轮询，是否有明确人工批准并同步恢复服务端契约、测试和全部文档？" |
| **产品品牌与历史技术标识是否同步改名** | 产品从“智医助理 / MedHermes”正式定稿为“全医慧助（PCIE）/ Primary Care Intelligent Expert”；旧名称同时承担 UI 品牌和已发布 SDK、深链、Header、Bundle Identifier，Windows MSI 又曾由不同 `productName` 隐式生成三套升级码，导致改名后新旧应用并存 | 用户可见品牌统一为“全医慧助（PCIE）”；SDK、深链、Header、Bundle Identifier 继续兼容旧标识；release 产物内部名保持 `PCIE`，Windows 通过自定义 WiX 模板展示中文名称，固定 `PCIE` 主升级码并额外迁移 `MedHermes` / 早期中文安装线，见 `PRODUCT.md`、`ARCHITECTURE.md` 与 `AGENTS.md` 硬约束 #9/#11 | "改的是用户可见品牌，还是内部产物/升级身份或既有 SDK 契约？是否保持稳定主升级码并覆盖全部历史安装线迁移？" |
| **main 与 two-chronic-diseases 是否共享发布版本线** | 两个方向从共同基点分叉后分别产生了 `1.2.99` 与 `1.3.x` tag；若继续按各自当前版本递增或复用同名 tag，会让 updater 面对版本倒退、旧 draft 和跨分支产物混合 | 已收敛为 `main` 是当前仓库唯一正式发布线并从 `1.4.0` 继续；`feature/two-chronic-diseases` 不合并回 main，后续移出当前仓库，不再占用当前正式通道和版本号，见 `ARCHITECTURE.md` 更新链路与 `AGENTS.md` 硬约束 #10 | "这次发版是否来自 main、版本是否高于全部历史公开版本、是否错误复用了另一分支的 tag / draft / 更新清单？" |
| **空病史字段是否允许自动补阴性模板** | 早期为追求病历字段完整，Builder 会在缺少询问或查体依据时自动补大段“否认……”和“未见异常”模板；随后为避免 AI 建议伪装成患者事实，曾收紧为空字段只显示未记录。2026-08-14 客户进一步明确允许既往史、个人史、家族史使用统一模板，也允许为了病例相关性和书写完整性生成候选阴性内容；首版为模板字段增加“模板预制”标签，后续走查认为该提示增加正文噪声 | 已收敛为：三类标准模板仍可进入可编辑草稿，内部继续识别模板以支持候选去重与安全规则，但正文不显示模板来源提示；只有诊断高度相关或安全关键的 AI 候选在原句上标记待核查，确认后更新为医生事实，见 `PRODUCT.md` 病历事实标记与必要补问约束及 `ARCHITECTURE.md` 共享结果页能力 | "这段模板内容是否仍可编辑且不增加来源标签？诊断相关重点是否继续在原句上标明待核查？" |
| **选中诊断的鉴别信息承载形态** | 最初只有“诊断鉴别”按钮并打开页面中央弹框；随后改为单行摘要 + hover 浮层，但信息被截断且依赖鼠标停留；再改为卡片内默认展开 3 条并高亮，扫读更快却长期撑高诊断卡、下推待鉴别与治疗内容，且单行入口与展开区重复；改为按钮锚定浮层后仍遗留 controller 三条截断和正文两行截断，造成“已展开却无法看全” | 已收敛为“主诊核查按钮锚定的持久浮层”：预取完成后自动打开，以文字色和字重高亮并完整展示全部核查项和每项全文，超高时只滚动清单；底部使用“已确认”文字按钮收起，不再显示滚动说明，且该动作不产生临床事实；同一上下文关闭后不再自动打开，点击核查状态入口可重开；共享结果页不再使用中央模态框，见 `PRODUCT.md` 与 `ARCHITECTURE.md` | "改动是否同时满足默认可见、全文可达、可快速扫读、不撑高卡片、可手动关闭和可重新打开？是否又恢复了条数/行数截断、背景色块或中央遮罩？" |
| **病历事实用独立标签还是原文内标记** | 首版把已明确事实和 AI 补问集中成正文下方的胶囊与卡片，来源清楚但重复原文、破坏连续阅读；改到原文内后又在每个候选片段末尾重复“AI补充·待核查”，长病历中标签密度过高；医生希望保持连续阅读，同时仍能识别 AI 补充、阳性事实和重点核对项 | 已收敛为“原文内语义标记 + 一条病历级说明 + 精简待核查汇总”：正式字段只包含已有或医生确认事实，AI 候选以 `AI` / `!`、虚线和文字样式标识，病历正文前统一说明来源和确认门禁，单个片段不再重复标签；点击原文标记使用锚点浮窗核查，汇总区只做计数、定位和处理，见 `PRODUCT.md` 病历事实标记与必要补问约束及 `ARCHITECTURE.md` 共享结果页能力 | "新增提示是在正式病历字段中，还是带来源标记且可丢弃的阅读层？全局说明是否存在，单个片段是否又重复来源标签？" |
| **病例字段是否保留独立反馈入口** | 早期为收集 AI 原文与医生修正差异，在每个病历字段旁加入反馈按钮和悬浮弹层；实际运行中医生没有使用，按钮反而遮挡正文并迫使字段长期预留右侧空间 | 已收敛为移除病例字段级反馈 UI、草稿与运行时提交接线，只保留诊断/治疗项级反馈与工作区顶栏主动问题反馈；字段修改判断继续用于日志与系统文本基线保护，见 `PRODUCT.md` 语音问诊反馈闭环约束与 `ARCHITECTURE.md` 反馈职责 | "这是医生真正使用的推荐反馈，还是准备重新把字段级反馈控件放回病历正文？" |
| **一键回写成功后是否自动弹出问题反馈** | 早期成功回执后强制进入整页评价浮层，医生必须提交反馈或点击“暂不反馈”才能结束结果页；实际门诊中这一额外步骤打断连续操作，且项目已有工作区顶栏主动问题反馈入口 | 已收敛为成功回执后提交最终日志并直接收起结果页，不再默认弹出问题反馈或整页评价；诊断/治疗项级反馈继续保留，综合问题由顶栏主动入口承接，见 `PRODUCT.md` 一键回写成功收尾约束 | "反馈是医生主动行为，还是被放在高频回写主流程中的强制收尾步骤？" |
| **一键回写是整张提交还是允许部分范围** | 原 `record-confirmed` 固定携带完整门诊病历、诊断和全部已选医嘱；医生反馈实际工作中需要保留 HIS 已有部分内容，只回填本次需要的字段或分组。若简单传空数组/空字符串，PHIS 可能把“不回写”误解为“清空”；首版又用“回写已选内容”替换主按钮文案，导致相邻选择入口和锚定浮层随宽度变化横向跳动 | 已收敛为“一条 batch 请求 + 显式 writebackScope + 未选字段彻底省略 + 固定一键回写 CTA”：门诊病历七字段可分别选择，诊断、用药、检查检验处置按组选择；只校验实际选择范围，未选内容保持 HIS 原值且不计为拒绝建议，选择状态由数量和浮层说明表达，主按钮不再动态改文案，见 `PRODUCT.md` 一键回写范围选择约束与 `api.md` record-confirmed | "当前 payload 中缺失字段表示保持原值，还是空值表示清空？scope 与实际 payload 是否完全一致？范围状态是否会通过改变主 CTA 尺寸造成布局跳动？" |

---

## 新增模板

```markdown
| **决策点** | 摇摆历史 | 当前状态 | "改动前必须问清的问题？" |
```

如果单行表格放不下，使用下面的展开模板：

```markdown
### DRIFT-XXX: 决策点

- **摇摆历史**: 从 A 到 B，再到 C，期间为什么反复。
- **当前状态**: 当前代码、产品或规则采用的立场。
- **改动前问**: 后续 agent 在动手前必须向人确认的问题。
- **收敛位置**: 如已固化，注明 `ARCHITECTURE.md` / `PRODUCT.md` / `AGENTS.md` 的位置；未固化则写"未收敛"。
```
